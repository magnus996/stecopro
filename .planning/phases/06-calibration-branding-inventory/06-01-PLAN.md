---
phase: 06-calibration-branding-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/simulator/params.ts
  - src/lib/simulator/engine.ts
  - src/lib/simulator/engine.test.ts
  - src/lib/dal.ts
  - src/db/seed.ts
  - src/app/(app)/dashboard/components/CurrentDrawChart.tsx
  - src/app/(app)/dashboard/page.tsx
  - scripts/verify-backfill.sh
autonomous: true

must_haves:
  truths:
    - "Simulated bunker current draw is 10-15 A when loaded and 4-6 A when running empty"
    - "Dosing bunker empty-detection threshold line on the chart sits at ~8 A, between the empty and loaded bands"
    - "A full production day generates ~135 bales total across the two shifts"
    - "Daily bale split is roughly 50% deink, 10% OCC, 8% tetra, 32% miks"
    - "OEE performance stays plausible (not artificially low) because the nominal matches the new ~80 bales/8h-shift rate"
    - "engine, dal, and verify-backfill tests/assertions all pass with the new calibration"
  artifacts:
    - path: "src/lib/simulator/params.ts"
      provides: "Calibrated current bands and bale rates"
      contains: "BALE_RATES_PER_SHIFT"
    - path: "src/lib/simulator/engine.ts"
      provides: "Engine reading rates/empty-current from params (no hardcoded numbers)"
    - path: "src/db/seed.ts"
      provides: "Calibrated seed: capacity 10 t/h, bunker nominalCurrentA 15"
  key_links:
    - from: "src/lib/simulator/engine.ts"
      to: "src/lib/simulator/params.ts"
      via: "imports BALE_RATES_PER_SHIFT + CURRENT_BUNKER_EMPTY_MIN/MAX"
      pattern: "BALE_RATES_PER_SHIFT|CURRENT_BUNKER_EMPTY"
    - from: "src/lib/dal.ts"
      to: "NOMINAL_BALES_PER_SHIFT"
      via: "value 80 consistent with new BALE_RATES_PER_SHIFT total"
      pattern: "NOMINAL_BALES_PER_SHIFT = 80"
---

<objective>
Recalibrate the simulator and dashboard to the real plant's numbers (SIMU-09): bunker
draws 10-15 A loaded / 4-6 A empty, the plant runs ~10 bales/h yielding ~135 bales/day
across two shifts, split 50/10/8/32 (deink/OCC/tetra/miks), and OEE performance uses a
consistent ~80-bales-per-8h-shift nominal so it is not depressed by a stale 120.

Purpose: The demo must mirror the customer's actual returpapir plant so the numbers are
credible in a sales conversation.
Output: Updated params/engine/seed constants, green engine + verify-backfill tests, and a
chart whose Y-axis and threshold line reflect the new low current bands.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@src/lib/simulator/params.ts
@src/lib/simulator/engine.ts
@src/lib/simulator/engine.test.ts
@src/lib/simulator/runner.ts
@src/lib/oee.ts
@scripts/verify-backfill.sh

# Calibration math (authoritative, user-supplied):
# - Bunker (11 kW motor): 10-15 A loaded, 4-6 A empty, threshold ~8 A.
# - ~10 bales/h running; SIMULATED availability ~90% → ~10 bales/h × 8h × 0.9 ≈ 72 (day)
#   + 7h evening ≈ 63 ⇒ ~135 bales/day total.
# - Nominal per 8h shift (100% availability) = 80 bales, split:
#     deink 40, occ 8, tetra 6, miks 26  (engine order is [deink, occ, tetra, miks])
# - NOMINAL_BALES_PER_SHIFT = 80 (sum of the rates above).
# OEE performance in src/lib/oee.ts already prorates nominal by run-time fraction of the
# FULL planned window, so a 7h evening shift is NOT penalised — only the nominal value
# needs to change, no oee.ts edit required.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalibrate params + engine (no hardcoded rates/current)</name>
  <files>src/lib/simulator/params.ts, src/lib/simulator/engine.ts</files>
  <action>
In src/lib/simulator/params.ts:
- Set the bunker current bands to the new calibration:
    CURRENT_BUNKER_FULL_MIN = 10, CURRENT_BUNKER_FULL_MAX = 15
    CURRENT_BUNKER_EMPTY_MIN = 4, CURRENT_BUNKER_EMPTY_MAX = 6
  Update the section comment ("400V IE3 class") to note the bunker is an 11 kW dosing motor
  drawing 10-15 A loaded / 4-6 A running empty (empty-detection threshold ~8 A). Leave
  CONVEYOR and PRESS bands unchanged.
- Replace BALE_RATES_PER_SHIFT with the new mix (keyed exactly as today):
    { deink: 40, occ: 8, tetra: 6, miks: 26 }
  Update the comment to state the total is 80 bales/8h-shift (~10 bales/h) and document the
  50/10/8/32 weight mix.
- Leave TARGET_AVAILABILITY = 0.9, P_STOP_PER_MINUTE, stop bands, and bunker-refill
  constants UNCHANGED (simulated availability stays ~90%).

In src/lib/simulator/engine.ts:
- Import BALE_RATES_PER_SHIFT, CURRENT_BUNKER_EMPTY_MIN, CURRENT_BUNKER_EMPTY_MAX from
  './params' (add to the existing import block).
- Replace the hardcoded `const baleRates = [45, 35, 25, 15]` (currently ~line 146) with a
  rates array sourced from params IN ENGINE ORDER [deink, occ, tetra, miks]:
    const baleRates = [
      BALE_RATES_PER_SHIFT.deink,
      BALE_RATES_PER_SHIFT.occ,
      BALE_RATES_PER_SHIFT.tetra,
      BALE_RATES_PER_SHIFT.miks,
    ]
  Update the adjacent comment to the new per-shift numbers (deink 40 / occ 8 / tetra 6 /
  miks 26). NOTE: engine abstract index order is [deink, occ, tetra, miks] (see comment at
  the baleRates line and runner.ts ABSTRACT_FRACTION_NAMES) — keep that order.
- Replace the hardcoded bunker-empty current `randFloat(rng, 5, 8)` (currently ~line 198)
  with `randFloat(rng, CURRENT_BUNKER_EMPTY_MIN, CURRENT_BUNKER_EMPTY_MAX)`.
- Do NOT change the conveyor/press reading logic or the state machine.
  </action>
  <verify>
Run: npx tsc --noEmit (must pass). Then grep to confirm no stale hardcoded numbers remain:
  grep -n "45, 35, 25, 15\|randFloat(rng, 5, 8)" src/lib/simulator/engine.ts  → no matches
  grep -n "deink: 40" src/lib/simulator/params.ts  → matches
  </verify>
  <done>params has bunker 10-15/4-6 A bands and {deink:40,occ:8,tetra:6,miks:26}; engine reads both rates and empty-current from params; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update engine tests + NOMINAL_BALES_PER_SHIFT; add bale-rate assertions</name>
  <files>src/lib/simulator/engine.test.ts, src/lib/dal.ts, scripts/verify-backfill.sh</files>
  <action>
In src/lib/dal.ts:
- Change `const NOMINAL_BALES_PER_SHIFT = 120` (currently ~line 318) to `= 80` and update
  the comment above it to: "Nominal bales per 8h shift = sum of BALE_RATES_PER_SHIFT
  (deink 40 + occ 8 + tetra 6 + miks 26 = 80)." Make NO other dal.ts changes in this plan.

In src/lib/simulator/engine.test.ts:
- The existing availability/idle/spread/boundary/determinism tests still hold (calibration
  did not change the state machine) — keep them.
- Add a new test block inside the `describe('simulateShift', ...)`:
    it('CURRENT_BANDS: bunker readings are 0, or 4-6 (empty), or 10-15 (loaded)', () => {
      const events = simulateShift({ startMs: dayBounds.startMs, endMs: dayBounds.endMs, seed: 42 })
      const bunker = events.filter(e => e.type === 'reading' && e.machineId === 0)
      for (const r of bunker) {
        if (r.type !== 'reading') continue
        const a = r.currentA
        const ok = a === 0 || (a >= 4 && a <= 6) || (a >= 10 && a <= 15)
        expect(ok).toBe(true)
      }
    })
- Add a bale-mix sanity test:
    it('BALE_MIX: deink is the largest fraction and total is plausible (~70-90 for an 8h shift)', () => {
      const events = simulateShift({ startMs: dayBounds.startMs, endMs: dayBounds.endMs, seed: 42 })
      const bales = events.filter(e => e.type === 'bale')
      const byFrac = [0,0,0,0]
      for (const b of bales) if (b.type === 'bale') byFrac[b.fractionId]++
      const total = byFrac.reduce((s,n)=>s+n,0)
      // engine order [deink, occ, tetra, miks]; deink (40) must dominate
      expect(byFrac[0]).toBeGreaterThan(byFrac[1])
      expect(byFrac[0]).toBeGreaterThan(byFrac[2])
      expect(byFrac[0]).toBeGreaterThanOrEqual(byFrac[3])
      expect(total).toBeGreaterThanOrEqual(60)
      expect(total).toBeLessThanOrEqual(95)
    })

In scripts/verify-backfill.sh (append a 7th assertion in the embedded TS, keeping the
existing pipe-delimited output style):
- After ASSERT6, add ASSERT7 computing total bale_events and asserting a 14-day total in a
  plausible range. ~135 bales/day × ~14 days but the most-recent day is partial and the
  oldest boundary varies, so use a wide band:
    const totalBales = (db.prepare('SELECT COUNT(*) AS n FROM bale_events').get() as any).n
    const balesPass = totalBales >= 1400 && totalBales <= 2200
    console.log('ASSERT7|' + (balesPass ? '1' : '0') + '|total_bales=' + totalBales + ' (expect 1400-2200 over ~14 days)')
- Add a matching parse branch in the bash `while` loop:
    elif [[ "$line" == ASSERT7\|* ]]; then
      val=$(echo "$line" | cut -d'|' -f2); detail=$(echo "$line" | cut -d'|' -f3-)
      check "$val" "7. Bale volume: $detail"
    fi
  (Insert before the closing `fi` of the existing chain — match the established structure.)
  </action>
  <verify>
Run: npm test --cache .npm-cache  (engine + oee + report tests all green, including the two
new bunker/bale-mix tests).
Then run the full backfill verify: bash scripts/verify-backfill.sh  → "RESULT: 7 passed, 0 failed".
  </verify>
  <done>NOMINAL_BALES_PER_SHIFT=80; vitest green incl. new current-band + bale-mix tests; verify-backfill.sh reports 7/7 with the new bale-volume assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Calibrate seed nominals + tune current-draw chart axis/threshold</name>
  <files>src/db/seed.ts, src/app/(app)/dashboard/components/CurrentDrawChart.tsx, src/app/(app)/dashboard/page.tsx</files>
  <action>
In src/db/seed.ts (tenant 1 plant + bunker machine only):
- plant1 nominalCapacityTph: change 12 → 10 (see the plants insert ~line 137).
- Doseringsbunker machine nominalCurrentA: change 45 → 15 (the bunker machine insert
  ~line 161). Leave conveyor (30) and press (75) nominals unchanged. Leave tenant 2
  rows unchanged.

In src/app/(app)/dashboard/components/CurrentDrawChart.tsx:
- The bunker now runs 4-6 A empty / 10-15 A loaded. Change the YAxis `domain={[0, 60]}`
  to `domain={[0, 20]}` so the low bands are readable.
- The empty-detection threshold ReferenceLine `y={8}` STAYS at 8 (it sits between the 4-6
  empty band and the 10-15 loaded band — the correct empty-detection cutoff). Keep its
  "Bunker tom" label.
- No other changes (conveyor/press are not on this chart).

In src/app/(app)/dashboard/page.tsx:
- The hint text under the chart already reads "Stiplet linje ved 8 A = grense for
  «Bunker tom»-deteksjon" — that is still correct, leave it. No change needed unless the
  text drifted; verify it still says 8 A.
  </action>
  <verify>
Run: npx tsc --noEmit (clean). Confirm edits:
  grep -n "nominalCapacityTph: 10" src/db/seed.ts  → matches
  grep -n "nominalCurrentA: 15" src/db/seed.ts  → matches (bunker)
  grep -n "domain={\[0, 20\]}" src/app/\(app\)/dashboard/components/CurrentDrawChart.tsx  → matches
  </verify>
  <done>Seed: capacity 10 t/h, bunker nominal 15 A; chart Y-axis 0-20 A with 8 A threshold preserved; tsc clean.</done>
</task>

</tasks>

<verification>
- npm test passes (engine current-band + bale-mix tests added and green; oee/report tests unchanged).
- bash scripts/verify-backfill.sh → 7/7 passed (availability still 85-95%, all 4 fractions non-zero, total bales 1400-2200).
- No hardcoded `[45, 35, 25, 15]` or `randFloat(rng, 5, 8)` remain in engine.ts.
- npx tsc --noEmit clean.
</verification>

<success_criteria>
- Bunker readings only ever fall in {0, 4-6, 10-15} A.
- A simulated day produces ~135 bales with deink dominating and the 50/10/8/32 shape.
- NOMINAL_BALES_PER_SHIFT (80) matches BALE_RATES_PER_SHIFT total, so OEE performance is not depressed.
- Chart Y-axis and 8 A threshold visibly frame the new low current bands.
</success_criteria>

<output>
After completion, create `.planning/phases/06-calibration-branding-inventory/06-01-SUMMARY.md`
</output>
