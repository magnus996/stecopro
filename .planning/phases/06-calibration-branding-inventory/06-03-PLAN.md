---
phase: 06-calibration-branding-inventory
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/db/schema.ts
  - src/lib/dal.ts
  - src/actions/inventory.ts
  - scripts/seed-shipments.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "A bale_shipments table exists (tenant-scoped) recording outbound bale shipments"
    - "Stock per fraction is computed as produced bale_events minus shipped baleCount"
    - "Registering a shipment reduces the computed stock for that fraction"
    - "A shipment cannot ship more bales than are currently in stock (server-side validation)"
    - "After demo:setup, each fraction shows a plausible in-stock count (tens of bales, not ~1900)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "bale_shipments table definition"
      contains: "bale_shipments"
    - path: "src/lib/dal.ts"
      provides: "getStockByFraction + getShipmentHistory accessors"
      contains: "getStockByFraction"
    - path: "src/actions/inventory.ts"
      provides: "registerShipment server action with zod + stock validation"
      exports: ["registerShipment"]
    - path: "scripts/seed-shipments.ts"
      provides: "Idempotent post-simulate shipment seeding (~95% of produced bales)"
  key_links:
    - from: "src/actions/inventory.ts"
      to: "bale_shipments"
      via: "tenant-scoped insert after stock check"
      pattern: "baleShipments"
    - from: "src/lib/dal.ts"
      to: "bale_shipments + bale_events"
      via: "COUNT(bale_events) - SUM(baleShipments.baleCount) per fraction"
      pattern: "baleShipments"
    - from: "package.json"
      to: "scripts/seed-shipments.ts"
      via: "demo:setup runs seed → simulate → seed-shipments"
      pattern: "seed-shipments"
---

<objective>
Add bale inventory tracking (INVT-01 backend): a tenant-scoped bale_shipments table, DAL
accessors that compute stock = produced − shipped per fraction, a validated server action
to register outbound shipments, and a post-simulate seeding step so the demo shows
realistic in-stock counts (tens of bales) rather than the full 14-day production.

Purpose: Plant owners track how many bales of each fraction are on hand and shippable.
Output: schema + drizzle push, DAL stock/history accessors, registerShipment action,
seed-shipments script wired into demo:setup.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/db/schema.ts
@src/actions/plant.ts
@scripts/simulate.ts
@src/lib/dal.ts   # patterns: verifySession() first, session.tenantId in every WHERE; getBaleCountsByFraction LEFT JOIN

# Session shape (from verifySession): { userId, tenantId, role } all available.
# Stock per fraction = COUNT(bale_events for fraction) − SUM(bale_shipments.baleCount for fraction).
# ORDERING WARNING: scripts/simulate.ts DELETEs tenant-scoped event data (incl. bale_events)
# and regenerates 14 days each run. Therefore shipments MUST be seeded as a PERCENTAGE of
# actually-produced bales AFTER simulate runs, and the seed step must be idempotent
# (delete this tenant's shipments first). demo:setup order becomes seed → simulate → seed-shipments.
</context>

<tasks>

<task type="auto">
  <name>Task 1: bale_shipments schema + drizzle push</name>
  <files>src/db/schema.ts</files>
  <action>
Append a new table to src/db/schema.ts following the existing table conventions (int pk
autoIncrement, tenantId NOT NULL FK, timestamps as integer mode):

  export const baleShipments = table('bale_shipments', {
    id: int().primaryKey({ autoIncrement: true }),
    tenantId: int('tenant_id').notNull().references(() => tenants.id),
    plantId: int('plant_id').notNull().references(() => plants.id),
    fractionId: int('fraction_id').notNull().references(() => fractions.id),
    baleCount: int('bale_count').notNull(),
    shippedAt: integer({ mode: 'timestamp' }).notNull(),
    note: text(),
    createdById: int('created_by_id').references(() => users.id),
    createdAt: integer({ mode: 'timestamp' }).notNull().$defaultFn(() => new Date()),
  }, (t) => [
    index('bale_shipments_plant_time_idx').on(t.plantId, t.shippedAt),
    index('bale_shipments_tenant_idx').on(t.tenantId),
    index('bale_shipments_fraction_idx').on(t.fractionId),
  ])

Note: shippedAt is stored via Drizzle timestamp mode (the codebase stores these as
ms-epoch integers and the planning note's "unix seconds" is the conceptual contract — keep
consistency with the existing tables which all use { mode: 'timestamp' }). createdById is
nullable so the seed script can insert without a user.

Then push the schema:  npm run db:push --cache .npm-cache  (accept defaults; drizzle-kit
push, the established dev-iteration tool per STATE.md).
  </action>
  <verify>
Run: npm run db:push --cache .npm-cache  (exits 0).
Confirm table exists:
  npx tsx -e "const D=require('better-sqlite3'); const d=new D(process.env.DB_FILE_NAME??'./stecopro.db'); console.log(d.prepare(\"SELECT name FROM sqlite_master WHERE type='table' AND name='bale_shipments'\").get())"
  → prints { name: 'bale_shipments' }
  npx tsc --noEmit  (clean).
  </verify>
  <done>bale_shipments table exists in the DB with tenant/plant/fraction FKs, baleCount, shippedAt, note, createdById; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Stock + history DAL accessors and registerShipment action</name>
  <files>src/lib/dal.ts, src/actions/inventory.ts</files>
  <action>
In src/lib/dal.ts (add baleShipments to the @/db/schema import list, then add two cached
accessors — both call verifySession() first and filter every query by session.tenantId):

  // Stock per fraction = produced bale_events − shipped baleCount, never below 0.
  export const getStockByFraction = cache(async (plantId: number) => {
    const session = await verifySession()
    // produced counts (reuse the LEFT JOIN shape so zero-bale fractions appear)
    const produced = await db.select({
        fractionId: fractions.id, name: fractions.name, sortOrder: fractions.sortOrder,
        count: sql<number>`count(${baleEvents.id})`,
      })
      .from(fractions)
      .leftJoin(baleEvents, and(
        eq(baleEvents.fractionId, fractions.id),
        eq(baleEvents.tenantId, session.tenantId)))
      .where(and(eq(fractions.tenantId, session.tenantId), eq(fractions.plantId, plantId)))
      .groupBy(fractions.id).orderBy(asc(fractions.sortOrder))
    // shipped sums keyed by fractionId
    const shippedRows = await db.select({
        fractionId: baleShipments.fractionId,
        shipped: sql<number>`coalesce(sum(${baleShipments.baleCount}),0)`,
      })
      .from(baleShipments)
      .where(and(eq(baleShipments.tenantId, session.tenantId), eq(baleShipments.plantId, plantId)))
      .groupBy(baleShipments.fractionId)
    const shippedMap = new Map(shippedRows.map(r => [r.fractionId, Number(r.shipped)]))
    return produced.map(p => ({
      fractionId: p.fractionId, name: p.name, sortOrder: p.sortOrder,
      produced: Number(p.count),
      shipped: shippedMap.get(p.fractionId) ?? 0,
      stock: Math.max(0, Number(p.count) - (shippedMap.get(p.fractionId) ?? 0)),
    }))
  })

  // Latest shipments (default 20) with fraction name, newest first.
  export const getShipmentHistory = cache(async (plantId: number, limit = 20) => {
    const session = await verifySession()
    return db.select({
        id: baleShipments.id, fractionId: baleShipments.fractionId,
        fractionName: fractions.name, baleCount: baleShipments.baleCount,
        shippedAt: baleShipments.shippedAt, note: baleShipments.note,
      })
      .from(baleShipments)
      .innerJoin(fractions, eq(fractions.id, baleShipments.fractionId))
      .where(and(eq(baleShipments.tenantId, session.tenantId), eq(baleShipments.plantId, plantId)))
      .orderBy(desc(baleShipments.shippedAt)).limit(limit)
  })

Make NO other dal.ts edits (NOMINAL_BALES_PER_SHIFT was already set to 80 in plan 01).

Create src/actions/inventory.ts (mirror src/actions/plant.ts conventions: 'use server',
zod, verifySession, revalidatePath):

  'use server'
  import { z } from 'zod'
  import { revalidatePath } from 'next/cache'
  import { verifySession, getStockByFraction } from '@/lib/dal'
  import { db } from '@/db'
  import { baleShipments } from '@/db/schema'

  const ShipmentSchema = z.object({
    plantId: z.coerce.number().int().positive(),
    fractionId: z.coerce.number().int().positive(),
    baleCount: z.coerce.number().int().positive('Antall må være et positivt heltall'),
    note: z.string().max(500).optional(),
  })

  export type RegisterShipmentState =
    | { success: true } | { errors: Record<string, string[]> } | undefined

  export async function registerShipment(
    prevState: RegisterShipmentState, formData: FormData,
  ): Promise<RegisterShipmentState> {
    const session = await verifySession()  // all authenticated roles may register (nav item is for ALL roles)
    const parsed = ShipmentSchema.safeParse({
      plantId: formData.get('plantId'),
      fractionId: formData.get('fractionId'),
      baleCount: formData.get('baleCount'),
      note: formData.get('note') || undefined,
    })
    if (!parsed.success) return { errors: parsed.error.flatten().fieldErrors as Record<string,string[]> }
    const { plantId, fractionId, baleCount, note } = parsed.data

    // Stock guard — cannot ship more than in stock (getStockByFraction is tenant-scoped)
    const stock = await getStockByFraction(plantId)
    const row = stock.find(s => s.fractionId === fractionId)
    if (!row) return { errors: { fractionId: ['Ukjent fraksjon'] } }
    if (baleCount > row.stock) {
      return { errors: { baleCount: [`Kan ikke sende ${baleCount} baler — kun ${row.stock} på lager`] } }
    }

    await db.insert(baleShipments).values({
      tenantId: session.tenantId, plantId, fractionId, baleCount,
      shippedAt: new Date(), note: note ?? null, createdById: session.userId,
    })
    revalidatePath('/inventory')
    revalidatePath('/dashboard')
    return { success: true }
  }
  </action>
  <verify>
Run: npx tsc --noEmit (clean — confirms accessor + action types compile, including the
getStockByFraction call from the action).
Confirm exports:
  grep -n "export const getStockByFraction\|export const getShipmentHistory" src/lib/dal.ts
  grep -n "export async function registerShipment" src/actions/inventory.ts
  </verify>
  <done>getStockByFraction returns {produced,shipped,stock} per fraction; getShipmentHistory returns latest shipments with names; registerShipment validates positive int + stock cap and inserts tenant-scoped; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: seed-shipments script + demo:setup wiring</name>
  <files>scripts/seed-shipments.ts, package.json</files>
  <action>
Create scripts/seed-shipments.ts modelled on scripts/simulate.ts's standalone-DB pattern
(own better-sqlite3 connection with WAL pragmas; NEVER import @/db/index). It must:
- Open the DB (DB_FILE_NAME ?? './stecopro.db').
- Discover tenant 1's plant (plantRows[0]) and its tenantId, same as simulate.ts.
- Idempotency: DELETE FROM bale_shipments WHERE tenant_id = ? (this tenant only) first.
- For each fraction of that plant, count produced bale_events
  (SELECT fraction_id, COUNT(*) FROM bale_events WHERE plant_id=? GROUP BY fraction_id),
  then ship out floor(produced * 0.95) as ONE shipment row per fraction so the remaining
  stock is ~5% (tens of bales, matching the target "20-80 in stock" feel). Skip fractions
  with 0 produced. Set shippedAt to a recent time (e.g. Date.now()), note = 'Demo-utsendelse',
  createdById = NULL.
- Use raw better-sqlite3 prepared statements (INSERT INTO bale_shipments
  (tenant_id, plant_id, fraction_id, bale_count, shippedAt, note, created_by_id, createdAt)
  VALUES (...)). Store shippedAt/createdAt as ms-epoch integers to match Drizzle timestamp mode.
- Print a summary: per-fraction produced / shipped / remaining stock.
- close() + process.exit(0); on error exit(1). Follow simulate.ts's then/catch structure.

In package.json scripts:
- Add:  "seed-shipments": "tsx scripts/seed-shipments.ts"
- Update demo:setup to:  "npm run db:seed && npm run db:simulate && npm run seed-shipments"
- Update db:reset the same way:  "npm run db:seed && npm run db:simulate && npm run seed-shipments"
  (so any reset path leaves realistic stock).
  </action>
  <verify>
Run the full demo reset: npm run demo:setup --cache .npm-cache  (exits 0, prints
seed-shipments summary).
Then assert remaining stock is plausible (tens, not ~1900) and non-negative:
  npx tsx -e "const D=require('better-sqlite3'); const d=new D(process.env.DB_FILE_NAME??'./stecopro.db'); const p=d.prepare('SELECT id FROM plants ORDER BY id LIMIT 1').get(); const rows=d.prepare('SELECT f.name, (SELECT COUNT(*) FROM bale_events be WHERE be.fraction_id=f.id) prod, COALESCE((SELECT SUM(bale_count) FROM bale_shipments bs WHERE bs.fraction_id=f.id),0) ship FROM fractions f WHERE f.plant_id=?').all(p.id); for(const r of rows){const stock=r.prod-r.ship; console.log(r.name, 'stock='+stock); if(stock<0||stock>500) process.exit(1);} console.log('OK')"
  → prints per-fraction stock and 'OK' (each 0-500, realistic tens for the demo plant).
  </verify>
  <done>demo:setup runs seed→simulate→seed-shipments; bale_shipments populated idempotently at ~95% of production; remaining per-fraction stock is small positive (tens), never negative.</done>
</task>

</tasks>

<verification>
- npm run db:push succeeds; bale_shipments table present.
- npx tsc --noEmit clean.
- npm run demo:setup completes and leaves plausible per-fraction stock (tens, non-negative).
- getStockByFraction, getShipmentHistory exported from dal.ts; registerShipment exported from actions/inventory.ts.
</verification>

<success_criteria>
- Stock = produced − shipped is computed correctly and never negative.
- registerShipment rejects over-stock and non-positive counts, inserts tenant-scoped rows otherwise.
- The demo dataset shows believable in-stock numbers after a one-command reset.
</success_criteria>

<output>
After completion, create `.planning/phases/06-calibration-branding-inventory/06-03-SUMMARY.md`
</output>
