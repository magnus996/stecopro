---
phase: 06-calibration-branding-inventory
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/lib/nav.ts
  - src/app/(app)/inventory/page.tsx
  - src/app/(app)/inventory/ShipmentForm.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/dashboard/components/LagerstatusCard.tsx
  - src/app/(app)/dashboard/components/ProduksjonIDagChart.tsx
  - scripts/e2e-phase6.sh
autonomous: true

must_haves:
  truths:
    - "All roles see a 'Lager' nav item linking to /inventory"
    - "The inventory page shows current stock per fraction (produced − shipped)"
    - "A form registers an outbound shipment (fraction, baleCount, optional note) and reduces stock"
    - "The inventory page lists the latest ~20 shipments as history"
    - "The dashboard shows a Lagerstatus widget with current stock per fraction"
    - "The dashboard shows a bar chart of bales produced today per fraction"
  artifacts:
    - path: "src/app/(app)/inventory/page.tsx"
      provides: "Lager page: stock table + form + history"
      min_lines: 40
    - path: "src/app/(app)/inventory/ShipmentForm.tsx"
      provides: "Client form bound to registerShipment via useActionState"
      contains: "registerShipment"
    - path: "src/app/(app)/dashboard/components/LagerstatusCard.tsx"
      provides: "Dashboard stock-per-fraction widget"
    - path: "src/app/(app)/dashboard/components/ProduksjonIDagChart.tsx"
      provides: "Recharts BarChart of today's bales per fraction"
      contains: "BarChart"
  key_links:
    - from: "src/app/(app)/inventory/ShipmentForm.tsx"
      to: "registerShipment"
      via: "useActionState(registerShipment, undefined)"
      pattern: "useActionState"
    - from: "src/app/(app)/inventory/page.tsx"
      to: "getStockByFraction + getShipmentHistory"
      via: "server-component data fetch"
      pattern: "getStockByFraction"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "getStockByFraction"
      via: "Lagerstatus widget data"
      pattern: "getStockByFraction|LagerstatusCard"
    - from: "src/lib/nav.ts"
      to: "/inventory"
      via: "NAV_ITEMS entry for all roles"
      pattern: "/inventory"
---

<objective>
Build the inventory UI (INVT-02, INVT-03, DASH-09): a "Lager" page with stock-per-fraction,
a shipment registration form, and shipment history; plus two dashboard widgets — a
Lagerstatus stock card and a Recharts bar chart of bales produced today per fraction.

Purpose: Plant staff register outbound shipments and see at a glance what is on hand and
what was produced today.
Output: /inventory route, ShipmentForm client component, LagerstatusCard +
ProduksjonIDagChart dashboard widgets, nav item, and an E2E script.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/lib/nav.ts
@src/app/(auth)/login/LoginForm.tsx   # useActionState + error-rendering pattern
@src/app/(app)/dashboard/page.tsx     # server page that fetches getDashboardData; already builds todayBales [{name,count}]
@src/app/(app)/dashboard/components/BaleCountsCard.tsx   # card markup pattern
@src/app/(app)/dashboard/components/CurrentDrawChart.tsx # Recharts ResponsiveContainer + isAnimationActive={false} pattern

# Backend from plan 03 (already exists): getStockByFraction(plantId) →
#   [{fractionId,name,sortOrder,produced,shipped,stock}], getShipmentHistory(plantId,limit),
#   and registerShipment(prevState, formData) server action in src/actions/inventory.ts.
# Today's per-fraction bales: getBaleCountsByFraction(plantId, todayStart, todayEnd) already
# exists; dashboard page.tsx already exposes data.baleCounts.today → todayBales [{name,count}].
</context>

<tasks>

<task type="auto">
  <name>Task 1: /inventory page + ShipmentForm + nav item</name>
  <files>src/lib/nav.ts, src/app/(app)/inventory/page.tsx, src/app/(app)/inventory/ShipmentForm.tsx</files>
  <action>
In src/lib/nav.ts: add a NAV_ITEMS entry for ALL roles, placed after Skiftrapporter:
  { label: 'Lager', href: '/inventory', roles: ['operator','produksjonsleder','admin','system_admin'] },

Create src/app/(app)/inventory/page.tsx (Server Component, mirrors dashboard/page.tsx
pattern — getCurrentUser, getPlants, take plants[0]; render "Ingen anlegg" fallback if
none). Fetch getStockByFraction(plant.id) and getShipmentHistory(plant.id, 20). Render:
- <h1>Lager — {plant.name}</h1>
- A "Lagerstatus" table: per fraction name | produsert | sendt | på lager (use the stock
  rows; show a total row like BaleCountsCard).
- The <ShipmentForm> client component, passing the fraction list
  ([{id,name,stock}] derived from stock rows) and plantId so the select + max hints work.
- A "Utsendelseshistorikk" table of the latest shipments: dato (shippedAt → Oslo
  'dd.MM HH:mm' via Intl.DateTimeFormat like dashboard/page.tsx), fraksjon (fractionName),
  antall (baleCount), notat (note ?? '—'). Serialise shippedAt to a string in the server
  component before passing to any client markup (history table can stay server-rendered).
Use the established card styling (rounded-lg border bg-white dark:bg-zinc-900 p-6).

Create src/app/(app)/inventory/ShipmentForm.tsx ('use client', mirrors LoginForm.tsx):
- useActionState(registerShipment, undefined) from '@/actions/inventory'.
- Props: { plantId: number; fractions: {id:number; name:string; stock:number}[] }.
- Hidden input name="plantId" value={plantId}.
- A <select name="fractionId"> listing fractions with their on-hand stock in the label
  e.g. `{f.name} ({f.stock} på lager)`.
- <input name="baleCount" type="number" min="1" required>.
- <textarea name="note"> (optional).
- Submit button "Registrer utsendelse" (disabled while isPending).
- Render field errors from state.errors (baleCount, fractionId) like LoginForm renders
  state.errors.email. On state.success, show a green "Utsendelse registrert" message.
  (revalidatePath in the action refreshes the page tables.)
  </action>
  <verify>
Run: npx tsc --noEmit (clean).
Confirm wiring:
  grep -n "/inventory" src/lib/nav.ts
  grep -n "getStockByFraction\|getShipmentHistory" src/app/\(app\)/inventory/page.tsx
  grep -n "useActionState\|registerShipment" src/app/\(app\)/inventory/ShipmentForm.tsx
  </verify>
  <done>'/inventory' nav item for all roles; page shows stock table + form + history; form posts to registerShipment with error/success rendering; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Lagerstatus widget + Produksjon i dag bar chart</name>
  <files>src/app/(app)/dashboard/components/LagerstatusCard.tsx, src/app/(app)/dashboard/components/ProduksjonIDagChart.tsx, src/app/(app)/dashboard/page.tsx</files>
  <action>
Create src/app/(app)/dashboard/components/LagerstatusCard.tsx (server component, mirrors
BaleCountsCard markup). Props: { rows: {name:string; stock:number}[] }. Title "Lagerstatus"
(uppercase tracking like other cards). Render a per-fraction table name | på lager with a
total row.

Create src/app/(app)/dashboard/components/ProduksjonIDagChart.tsx ('use client', mirrors
CurrentDrawChart imports). Props: { data: {name:string; count:number}[] }. Render a small
Recharts BarChart in a ResponsiveContainer (height ~13rem / h-52) with XAxis dataKey="name",
YAxis, Tooltip, and a single <Bar dataKey="count" fill="#3b82f6" isAnimationActive={false} />.
Show an "Ingen produksjon i dag" empty state when data is empty or all counts are 0.

In src/app/(app)/dashboard/page.tsx:
- Import the two new components and getStockByFraction from '@/lib/dal'.
- After `const data = await getDashboardData(plant.id)`, also fetch
  `const stock = await getStockByFraction(plant.id)` and map to
  `const stockRows = stock.map(s => ({ name: s.name, stock: s.stock }))`.
- `todayBales` already exists ([{name,count}]) — reuse it for the chart.
- Add <LagerstatusCard rows={stockRows} /> into the existing 2-column widget grid
  (the `grid ... lg:grid-cols-2` block), alongside the other cards.
- Add a full-width section (same card wrapper as the current-draw chart) titled
  "Produksjon i dag" containing <ProduksjonIDagChart data={todayBales} />. Place it near
  the current-draw chart section.
Do not alter the calibration/current-draw chart logic from plan 01.
  </action>
  <verify>
Run: npx tsc --noEmit (clean).
Confirm:
  grep -n "LagerstatusCard\|ProduksjonIDagChart\|getStockByFraction" src/app/\(app\)/dashboard/page.tsx
  grep -n "BarChart" src/app/\(app\)/dashboard/components/ProduksjonIDagChart.tsx
  </verify>
  <done>Dashboard renders a Lagerstatus stock card and a Produksjon-i-dag bar chart of today's bales per fraction; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: E2E script for inventory + dashboard widgets</name>
  <files>scripts/e2e-phase6.sh</files>
  <action>
Create scripts/e2e-phase6.sh modelled on scripts/e2e-phase3.sh (zsh, BASE override, check()
helper, login() via /api/auth/login, cookie jar). Requires a dev server + demo:setup data.
Assertions:
- SC0: /login returns 200.
- SC1: operator login succeeds ('"ok":true').
- SC2: GET /inventory returns 200 and HTML contains 'Lager', 'Lagerstatus',
  'Utsendelseshistorikk', and at least one fraction name (e.g. 'Deink').
- SC3: dashboard HTML contains 'Lagerstatus' and 'Produksjon i dag'.
- SC4: the 'Lager' nav link (href="/inventory") appears in the dashboard HTML.
- SC5: a register-shipment round-trip via tsx + better-sqlite3 (mirror the e2e-phase5
  user-CRUD inline-tsx style noted in STATE.md): read a fraction's current stock, invoke
  the validation contract directly — insert a small shipment row for that fraction with
  bale_count well under stock, then assert getStockByFraction-equivalent stock dropped by
  that amount; also assert an over-stock attempt is rejected by checking the action's guard
  logic is honoured (compute produced−shipped and confirm count<=stock before insert).
  Keep it deterministic and clean up the inserted test row afterward (DELETE the row by id)
  so re-runs are idempotent.
Print "RESULT: $pass passed, $fail failed" and exit non-zero on any fail.
  </action>
  <verify>
Start a dev server on a unique port and run the script, then stop the server:
  PORT=3006 npm run dev --cache .npm-cache  (background) ; wait for /login 200 ;
  BASE=http://localhost:3006 zsh scripts/e2e-phase6.sh  → "RESULT: N passed, 0 failed" ;
  then stop the dev server.
(Ensure demo:setup has been run so stock + fractions exist.)
  </verify>
  <done>e2e-phase6.sh passes all assertions against a running dev server (inventory page, dashboard widgets, nav link, shipment stock round-trip); dev server stopped after.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit clean.
- /inventory renders stock table, shipment form, and history; nav 'Lager' item present for all roles.
- Dashboard shows Lagerstatus card + Produksjon-i-dag bar chart.
- zsh scripts/e2e-phase6.sh → all assertions pass against a dev server, server stopped after.
</verification>

<success_criteria>
- A user can open Lager, see stock per fraction, register a shipment, and watch stock drop + the shipment appear in history.
- The dashboard surfaces both current stock and today's production per fraction.
</success_criteria>

<output>
After completion, create `.planning/phases/06-calibration-branding-inventory/06-04-SUMMARY.md`
</output>
