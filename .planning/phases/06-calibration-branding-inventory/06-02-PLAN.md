---
phase: 06-calibration-branding-inventory
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/layout.tsx
  - src/app/(auth)/login/LoginForm.tsx
autonomous: true

must_haves:
  truths:
    - "The Steco logo appears in the app shell sidebar instead of the 'Stecopro' text wordmark"
    - "The Steco logo appears in the mobile top bar instead of the text wordmark"
    - "The Steco logo appears on the login page instead of the 'StecoPro' h1 text"
    - "The white logo sits on a dark background so it is legible on the otherwise light surfaces"
  artifacts:
    - path: "public/logo-hvit.png"
      provides: "White Steco wordmark (269x57)"
    - path: "src/app/(app)/layout.tsx"
      provides: "Logo in sidebar + mobile header"
      contains: "logo-hvit"
    - path: "src/app/(auth)/login/LoginForm.tsx"
      provides: "Logo on login card"
      contains: "logo-hvit"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "/logo-hvit.png"
      via: "next/image src"
      pattern: "logo-hvit"
    - from: "src/app/(auth)/login/LoginForm.tsx"
      to: "/logo-hvit.png"
      via: "next/image src"
      pattern: "logo-hvit"
---

<objective>
Replace the "Stecopro" / "StecoPro" text wordmarks with the real Steco logo (BRND-01) in
the app shell (sidebar + mobile header) and on the login page.

Purpose: The demo should look like Steco's product, not a placeholder.
Output: next/image-rendered logo in three locations, each on a dark backing so the white
PNG is legible against the light zinc surfaces.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/app/(app)/layout.tsx
@src/app/(auth)/login/LoginForm.tsx

# Asset: public/logo-hvit.png already exists — a 269x57 WHITE wordmark.
# IMPORTANT: the shell sidebar/header and login card are LIGHT surfaces
# (bg-white / bg-zinc-50). A white logo would be invisible, so it MUST sit in a
# dark container (e.g. bg-zinc-900) for contrast. Use next/image (already used
# nowhere yet, but it's the Next.js default and needs explicit width/height).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logo in app shell (sidebar + mobile header)</name>
  <files>src/app/(app)/layout.tsx</files>
  <action>
Import next/image at the top: `import Image from 'next/image'`.

Sidebar wordmark (currently the `<span>Steco<span>pro</span></span>` inside the
`border-b ... px-4 py-5` div, ~lines 32-36):
- Replace the text span with the logo. Wrap it in a dark backing so the white PNG shows:
  keep the existing bordered container but give it a dark background, e.g. change that
  div to `className="border-b border-zinc-200 bg-zinc-900 px-4 py-5 dark:border-zinc-800"`
  and inside render:
    <Image src="/logo-hvit.png" alt="Steco" width={150} height={32}
      style={{ height: 'auto', width: '150px' }} priority />
  (Scale 269x57 down to width 150 keeping aspect; `height: auto` avoids distortion warnings.)

Mobile top bar wordmark (the `<span>` inside the `<header ...>` ~lines 59-62):
- Replace the text span with a small dark pill containing the logo:
    <div className="rounded bg-zinc-900 px-2 py-1">
      <Image src="/logo-hvit.png" alt="Steco" width={110} height={23}
        style={{ height: 'auto', width: '110px' }} priority />
    </div>
- Leave the user name / role block in the header untouched.

Do not change Nav, LogoutButton, or the main content area.
  </action>
  <verify>
Run: npx tsc --noEmit (clean). Confirm:
  grep -c "logo-hvit" src/app/\(app\)/layout.tsx  → 2
  grep -c "bg-zinc-900" src/app/\(app\)/layout.tsx → at least 2 (sidebar + mobile pill)
  </verify>
  <done>Sidebar and mobile header render the white logo on a dark background; no text wordmark remains in layout.tsx; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Logo on login page</name>
  <files>src/app/(auth)/login/LoginForm.tsx</files>
  <action>
Import next/image: `import Image from 'next/image'`.

In the "Logo / title" block (~lines 16-20), replace the
`<h1 ...>StecoPro</h1>` with the logo on a dark backing centred in the card:
    <div className="mb-8 text-center">
      <div className="mx-auto mb-3 inline-flex rounded-lg bg-zinc-900 px-4 py-3">
        <Image src="/logo-hvit.png" alt="Steco" width={170} height={36}
          style={{ height: 'auto', width: '170px' }} priority />
      </div>
      <p className="mt-1 text-sm text-zinc-500">Logg inn for å fortsette</p>
    </div>
Keep the rest of the form (email/password/submit/demo-hint) unchanged.
  </action>
  <verify>
Run: npx tsc --noEmit (clean). Confirm:
  grep -c "logo-hvit" src/app/\(auth\)/login/LoginForm.tsx  → 1
  grep -c "StecoPro" src/app/\(auth\)/login/LoginForm.tsx     → 0 (h1 wordmark gone)
  </verify>
  <done>Login card shows the white logo on a dark pill; the StecoPro h1 text is gone; tsc clean.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit clean.
- `logo-hvit` referenced in both layout.tsx (x2) and LoginForm.tsx (x1).
- No remaining "Stecopro"/"StecoPro" text wordmark in the shell or login (the document <title> in src/app/layout.tsx metadata is out of scope and may keep the name).
</verification>

<success_criteria>
- All three brand surfaces (sidebar, mobile header, login) display the Steco logo.
- The white logo is legible thanks to a dark container on each light surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-calibration-branding-inventory/06-02-SUMMARY.md`
</output>
