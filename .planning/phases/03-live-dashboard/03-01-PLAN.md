---
phase: 03-live-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/time.ts
  - src/lib/simulator/time.ts
  - package.json
  - package-lock.json
autonomous: true

must_haves:
  truths:
    - "Dashboard and reports can import Oslo shift helpers from a non-simulator shared location"
    - "Existing simulator code and tests still compile and pass after the move"
    - "Recharts is installed and resolvable for client chart components"
  artifacts:
    - path: "src/lib/time.ts"
      provides: "Canonical osloHour/getShiftType/getShiftBoundsUtc shared helpers"
      contains: "export function getShiftType"
    - path: "src/lib/simulator/time.ts"
      provides: "Backward-compatible re-export so simulator imports keep working"
      contains: "export"
  key_links:
    - from: "src/lib/simulator/time.ts"
      to: "src/lib/time.ts"
      via: "re-export"
      pattern: "from '\\.\\./time'"
    - from: "src/lib/simulator/runner.ts"
      to: "getShiftBoundsUtc"
      via: "existing import './time' resolves through re-export"
      pattern: "getShiftBoundsUtc"
---

<objective>
Promote the Oslo-timezone shift helpers to a shared, non-simulator location and install the charting library. This is the foundation both the dashboard DAL (plan 03) and Phase 4 reports will build on.

Purpose: OEE and shift-attribution logic must be reusable by the dashboard and reports without importing from the simulator package. Recharts must be available for the current-draw chart.
Output: `src/lib/time.ts` as the canonical home for `osloHour`/`getShiftType`/`getShiftBoundsUtc`; `src/lib/simulator/time.ts` reduced to a re-export; recharts in dependencies.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-dashboard/03-RESEARCH.md
@src/lib/simulator/time.ts
@src/lib/simulator/runner.ts
@src/lib/simulator/engine.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move shift helpers to src/lib/time.ts and re-export from simulator</name>
  <files>src/lib/time.ts, src/lib/simulator/time.ts</files>
  <action>
Create `src/lib/time.ts` containing the FULL canonical implementations of `osloHour`, `getShiftType`, and `getShiftBoundsUtc` — copy the exact current bodies from `src/lib/simulator/time.ts` verbatim (including the JSDoc comments). These functions use `Intl.DateTimeFormat`/`toLocaleString` with `Europe/Oslo` and are DST-correct; do not change the logic.

Then REPLACE the contents of `src/lib/simulator/time.ts` with a single re-export line so existing simulator imports keep resolving:
```ts
// Canonical implementations now live in src/lib/time.ts (shared by dashboard + reports).
export { osloHour, getShiftType, getShiftBoundsUtc } from '../time'
```

Do NOT modify `src/lib/simulator/runner.ts` or `src/lib/simulator/engine.test.ts` — they import from `./time` which now re-exports. The whole point is backward compatibility through the re-export, so their import paths stay untouched.
  </action>
  <verify>
`npm test` passes (engine.test.ts imports getShiftType/osloHour/getShiftBoundsUtc from './time' and still resolves). Run: `npm test`.
Also confirm the re-export shape: `grep -q "from '../time'" src/lib/simulator/time.ts && grep -q "export function getShiftType" src/lib/time.ts && echo OK`.
  </verify>
  <done>`src/lib/time.ts` holds the real implementations; `src/lib/simulator/time.ts` is a one-line re-export; `npm test` is green; no changes to runner.ts or engine.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Install recharts</name>
  <files>package.json, package-lock.json</files>
  <action>
Install Recharts as a runtime dependency. The global npm cache is broken in this sandbox — you MUST use the project-local cache flag:
```bash
npm install --cache .npm-cache recharts
```
Recharts 3.x declares a React `^19.0.0` peer dependency (React 19.2.4 is installed), so NO `--legacy-peer-deps` flag is needed. If install errors with a peer-dependency complaint, re-read 03-RESEARCH.md "Standard Stack" — do not silently add `--legacy-peer-deps`; investigate first.
Do NOT add any other packages — the rest of the stack (next, react, drizzle, tailwind) is already present.
  </action>
  <verify>
`npm ls recharts --depth=0` shows recharts resolved (a 3.x version). Run: `node -e "require.resolve('recharts'); console.log('recharts OK')"`.
  </verify>
  <done>recharts appears in package.json dependencies and resolves via require.resolve.</done>
</task>

</tasks>

<verification>
- `npm test` passes (simulator tests resolve helpers through the re-export).
- `src/lib/time.ts` exports osloHour, getShiftType, getShiftBoundsUtc with real bodies.
- `src/lib/simulator/time.ts` is a re-export from '../time'.
- recharts resolves.
</verification>

<success_criteria>
Shared time module exists and is the canonical source; simulator still works through the re-export; recharts installed and resolvable. Foundation ready for the dashboard DAL (plan 03) and chart UI (plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-dashboard/03-01-SUMMARY.md`.
</output>
