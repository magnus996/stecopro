---
phase: 03-live-dashboard
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/dashboard/components/AutoRefresh.tsx
  - src/app/(app)/dashboard/components/PlantStatusCard.tsx
  - src/app/(app)/dashboard/components/OeeCard.tsx
  - src/app/(app)/dashboard/components/BaleCountsCard.tsx
  - src/app/(app)/dashboard/components/RecentStopsCard.tsx
  - src/app/(app)/dashboard/components/CurrentDrawChart.tsx
  - scripts/e2e-phase3.sh
autonomous: true

must_haves:
  truths:
    - "Dashboard shows live plant state: Kjører / Kjører – Bunker tom (running empty) / Stanset – {årsak} / Utenfor skift / Ingen data"
    - "OEE for the current shift is shown with Tilgjengelighet/Ytelse/Kvalitet breakdown and a visible OEE definition"
    - "Bale counts per fraction are shown for the current shift AND for today (since 07:00)"
    - "A current-draw chart for the doseringsbunker renders with a 'Bunker tom' empty-detection threshold reference"
    - "Recent stops are listed with start, duration, and reason"
    - "Uptime for current shift and today, and throughput vs nominal capacity, are shown"
    - "The dashboard auto-refreshes while the simulator runs (router.refresh on interval)"
  artifacts:
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Server component that calls getDashboardData and renders all widgets"
      contains: "getDashboardData"
    - path: "src/app/(app)/dashboard/components/AutoRefresh.tsx"
      provides: "'use client' polling component calling router.refresh()"
      contains: "router.refresh"
    - path: "src/app/(app)/dashboard/components/CurrentDrawChart.tsx"
      provides: "'use client' Recharts area chart with Bunker tom threshold"
      contains: "use client"
    - path: "src/app/(app)/dashboard/components/PlantStatusCard.tsx"
      provides: "Live state card with colour indicator + Norwegian labels"
      contains: "Bunker tom"
    - path: "src/app/(app)/dashboard/components/OeeCard.tsx"
      provides: "OEE A/P/Q breakdown + visible definition text"
      contains: "Tilgjengelighet"
    - path: "src/app/(app)/dashboard/components/BaleCountsCard.tsx"
      provides: "Per-fraction bale counts for current shift and today"
      contains: "I dag"
    - path: "src/app/(app)/dashboard/components/RecentStopsCard.tsx"
      provides: "Recent stops list with start/duration/reason"
      contains: "Varighet"
    - path: "scripts/e2e-phase3.sh"
      provides: "curl+grep E2E asserting dashboard widgets/markers in HTML"
      contains: "/api/auth/login"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/lib/dal.ts getDashboardData"
      via: "await getDashboardData(plantId) in the server component"
      pattern: "getDashboardData"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "CurrentDrawChart"
      via: "passes serialised (string/number) readings as props to the client chart"
      pattern: "CurrentDrawChart"
    - from: "src/app/(app)/dashboard/components/AutoRefresh.tsx"
      to: "next/navigation useRouter"
      via: "setInterval(() => router.refresh(), interval)"
      pattern: "useRouter"
---

<objective>
Build the live dashboard UI: replace the placeholder page with a server component that fetches dashboard data and renders the status, OEE, bale-count, current-draw, and recent-stops widgets, with client-side auto-refresh. Verify end-to-end against the running dev server.

Purpose: This is the user-visible payoff of Phase 3 — an operator sees what the plant is doing right now without walking the floor.
Output: New dashboard page + widget components + Recharts chart + AutoRefresh + a curl-based E2E script.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-dashboard/03-RESEARCH.md
@src/app/(app)/dashboard/page.tsx
@scripts/e2e-phase1.sh
@src/lib/dal.ts
# 03-03 added getDashboardData; 03-01 installed recharts + src/lib/time.ts; 03-02 added src/lib/oee.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client leaf components — AutoRefresh and CurrentDrawChart</name>
  <files>src/app/(app)/dashboard/components/AutoRefresh.tsx, src/app/(app)/dashboard/components/CurrentDrawChart.tsx</files>
  <action>
Create the two `'use client'` leaf components (Recharts and useRouter are browser-side — RESEARCH Pitfall 1).

AutoRefresh.tsx — exactly as RESEARCH Pattern 2: `'use client'`, `useRouter` from 'next/navigation', `useEffect` setting a `setInterval(() => router.refresh(), intervalMs)` cleared on unmount. Default `intervalMs = 30_000`. Renders `null`. Props: `{ intervalMs?: number }`.

CurrentDrawChart.tsx — `'use client'` Recharts AreaChart per RESEARCH "Code Examples". Props: `{ data: { label: string; currentA: number }[] }` (already-serialised: label is 'HH:mm' Oslo time, currentA a number — the PAGE does the formatting). Wrap in `ResponsiveContainer width="100%" height={200}` inside a parent div with an explicit height class like `h-52` (RESEARCH Pitfall 4 — ResponsiveContainer needs a sized parent). YAxis domain [0,60] unit ' A'. Add a `ReferenceLine y={8}` dashed amber with label 'Bunker tom' (8A = CURRENT_BUNKER_EMPTY_MAX from params.ts — the empty-detection threshold). `isAnimationActive={false}` so polling refreshes don't re-animate. Tooltip formats `${v.toFixed(1)} A`. Handle empty data array gracefully (render a 'Ingen måledata' placeholder when data.length === 0).
  </action>
  <verify>
`npx tsc --noEmit` passes. Both files start with `'use client'`. `grep -q "router.refresh" src/app/(app)/dashboard/components/AutoRefresh.tsx && grep -q "ReferenceLine" src/app/(app)/dashboard/components/CurrentDrawChart.tsx && echo OK`.
  </verify>
  <done>AutoRefresh polls via router.refresh on an interval; CurrentDrawChart renders an area chart with a 'Bunker tom' threshold line and handles empty data.</done>
</task>

<task type="auto">
  <name>Task 2: Server widget cards and the dashboard page</name>
  <files>src/app/(app)/dashboard/page.tsx, src/app/(app)/dashboard/components/PlantStatusCard.tsx, src/app/(app)/dashboard/components/OeeCard.tsx, src/app/(app)/dashboard/components/BaleCountsCard.tsx, src/app/(app)/dashboard/components/RecentStopsCard.tsx</files>
  <action>
Create four server (non-'use client') card components that receive plain props, and rewrite the page to compose them. Reuse the existing zinc/Tailwind card styling from the current page.tsx (rounded-lg border bg-white dark:bg-zinc-900 p-6). All user-facing text in Norwegian; code/comments English.

PlantStatusCard.tsx — props `{ state, reason }`. Map state -> label + colour dot (RESEARCH Pattern 5 labels):
  running -> 'Kjører' (green), running_empty -> 'Kjører – Bunker tom' (amber), stopped -> `Stanset – ${reason}` (red), outside_shift -> 'Utenfor skift' (zinc), no_data -> 'Ingen data' (zinc). Title 'Anleggsstatus'. Render a coloured status dot + the label prominently.

OeeCard.tsx — props `{ oee: OeeResult | null }`. Title 'OEE – Gjeldende skift'. If null, show 'Utenfor skift'. Else show the big OEE % (oee.oee*100, 1 decimal) and three sub-metrics: Tilgjengelighet (availability), Ytelse (performance), Kvalitet (quality), each as a percentage. Include the VISIBLE definition text: 'OEE = Tilgjengelighet × Ytelse × Kvalitet. Tilgjengelighet = driftstid/planlagt tid. Ytelse = faktisk balerate/nominell rate. Kvalitet = konfigurerbar faktor (95 %).' Also show current-shift uptime (runSeconds/plannedSeconds as %) and today uptime.

BaleCountsCard.tsx — props `{ currentShift: {name,count}[], today: {name,count}[] }`. Title 'Baler produsert'. Two labelled columns/sections: 'Gjeldende skift' and 'I dag', each a small table of fraction name -> count, plus a total row. Fractions render in sortOrder (Deink, Tetra/emballasjepapp, OCC, Miks) — the DAL already orders them.

RecentStopsCard.tsx — props `{ stops: {startAt:string, durationMin:number, reason:string|null, stopType:string}[] }`. Title 'Siste stopp'. Table columns: Startet | Varighet | Årsak. Format duration as minutes ('X min'). If reason null show '—'. Empty state 'Ingen stopp registrert'.

page.tsx — rewrite the placeholder. Keep it a Server Component (no 'use client'). Steps:
  - `const user = await getCurrentUser(); if (!user) return null` (existing guard).
  - `const plants = await getPlants(); const plant = plants[0]` (RESEARCH open-q 2: default to first plant; if no plant, render a 'Ingen anlegg' message).
  - `const data = await getDashboardData(plant.id)`.
  - SERIALISE before passing to client/leaf props (RESEARCH anti-pattern: no Date objects across the boundary). For currentDraw, map each reading to `{ label: <'HH:mm' Oslo via Intl.DateTimeFormat('no',{timeZone:'Europe/Oslo',hour:'2-digit',minute:'2-digit'}).format(recordedAt)>, currentA }`. For recentStops, compute durationMin = Math.round(((endAt ?? now) - startAt)/60000) and pass startAt as an 'HH:mm' (or 'dd.MM HH:mm') string. For bale counts pass `{name,count}` arrays.
  - Render heading with plant name, `<AutoRefresh />` (renders nothing), then a responsive grid (grid-cols-1 lg:grid-cols-2 gap-6): PlantStatusCard, OeeCard, BaleCountsCard, RecentStopsCard, and a full-width section titled 'Strømtrekk – Doseringsbunker' wrapping `<CurrentDrawChart data={...} />`. Add a throughput indicator ('Kapasitetsutnyttelse') showing nominalCapacityTph and the actual-vs-expected ratio from data.throughput.
  - Add `export const dynamic = 'force-dynamic'` is NOT required (verifySession's cookies() already forces dynamic) — do not add it; rely on the DAL pattern (RESEARCH Pattern 3 / Pitfall 2). Add a brief comment noting this.

Remove the old placeholder cards (Innlogget kontekst / plant list / 'Live anleggsdata kommer i fase 3' notice).
  </action>
  <verify>
`npx tsc --noEmit` passes and `npm run build` succeeds (no 'window is not defined' — confirms chart is correctly client-only). Confirm markers: `grep -q "getDashboardData" src/app/(app)/dashboard/page.tsx && grep -q "CurrentDrawChart" src/app/(app)/dashboard/page.tsx && grep -q "AutoRefresh" src/app/(app)/dashboard/page.tsx && echo OK`.
  </verify>
  <done>The dashboard page composes all widgets from getDashboardData with serialised props, no Date objects crossing to client components, and builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: E2E verification script against the running dev server</name>
  <files>scripts/e2e-phase3.sh</files>
  <action>
Create `scripts/e2e-phase3.sh` modelled on `scripts/e2e-phase1.sh` (same zsh header, `check()` helper, BASE=http://localhost:3000, login via POST /api/auth/login with curl cookie jar). The script assumes the dev server is running with the simulator (instrumentation live mode) and the DB seeded+simulated.

Before running assertions, the EXECUTOR must ensure data freshness: the simulator advances readings in real time only while `npm run dev` is running. Steps the script (or the executor in the plan) performs:
  1. Run `npm run db:reset` is NOT done here (destructive); instead document in a comment that the dev server must be running. The script focuses on asserting HTML markers.
  2. SC0: `curl /login` returns 200 (app up).
  3. Login as operator@steco-demo.no (password demo123) -> cookie jar; assert '"ok":true'.
  4. Fetch `/dashboard` HTML with the cookie. Assert the presence of the key widget markers (Norwegian text rendered server-side, so present in raw HTML):
     - 'Anleggsstatus'
     - 'OEE' AND 'Tilgjengelighet' AND 'Ytelse' AND 'Kvalitet'
     - the OEE definition substring 'Tilgjengelighet × Ytelse × Kvalitet'
     - 'Baler produsert' AND 'Gjeldende skift' AND 'I dag'
     - 'Strømtrekk' AND 'Doseringsbunker'
     - 'Siste stopp' AND 'Varighet'
     - 'Kapasitetsutnyttelse'
     - exactly one plant-state label present (one of 'Kjører', 'Stanset', 'Utenfor skift', 'Ingen data', 'Bunker tom') — assert at least one matches.
  5. Assert auto-refresh is wired: the rendered HTML / client bundle references the dashboard route's client component. Since AutoRefresh renders null, assert instead that the page is a dynamic render by fetching twice and confirming 200 both times, AND grep the built client chunk is impractical — instead assert the chart container marker is present (the chart is client-rendered, so assert the server-rendered wrapper text 'Strømtrekk – Doseringsbunker' exists, already covered). Document that live-movement is verified manually.
  6. Tenant isolation regression: login as bruker@isolasjonstest.no and assert the dashboard does NOT 500 and does NOT show 'Returpapir Linje 1' (that tenant has no plant — assert a graceful 'Ingen anlegg' or absence of the demo plant name, no crash).
  7. Print 'RESULT: X passed, Y failed' and exit non-zero on any failure (same pattern as phase1).

Make the script executable (`chmod +x`).

EXECUTION: Start the dev server in the background (`npm run dev`), wait for it to be ready (poll /login until 200, max ~60s), run `./scripts/e2e-phase3.sh`, capture the result, then stop the dev server. All assertions must pass.
  </action>
  <verify>
With dev server running: `./scripts/e2e-phase3.sh` exits 0 and prints all checks passed. The script is executable and uses /api/auth/login + cookie jar like phase1.
  </verify>
  <done>e2e-phase3.sh asserts every dashboard widget marker (status, OEE+breakdown+definition, bale counts shift+today, current-draw, recent stops, throughput), confirms a plant-state label renders, verifies no crash for the isolation tenant, and passes against the running dev server.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (chart correctly client-only, no window errors).
- `npx tsc --noEmit` clean.
- `./scripts/e2e-phase3.sh` passes against `npm run dev` with the simulator running.
- All Norwegian widget markers present in the dashboard HTML; OEE definition visible; bale counts for shift AND today; current-draw chart wrapper; recent stops table; auto-refresh component mounted.
</verification>

<success_criteria>
An operator opening /dashboard sees the live plant state (including 'Bunker tom' running-empty), OEE A/P/Q with a visible definition, per-fraction bale counts for the current shift and today, the doseringsbunker current-draw chart with empty-detection threshold, a recent-stops list, and throughput vs nominal capacity — and the view refreshes itself while the simulator runs. All six Phase 3 success criteria from ROADMAP are met and machine-verified by e2e-phase3.sh.
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-dashboard/03-04-SUMMARY.md`.
</output>
