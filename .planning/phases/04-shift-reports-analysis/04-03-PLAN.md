---
phase: 04-shift-reports-analysis
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(app)/reports/page.tsx
  - src/app/(app)/reports/components/DateRangeForm.tsx
  - src/app/(app)/reports/components/OeeTrendChart.tsx
  - src/app/(app)/reports/components/ParetoChart.tsx
  - src/app/(app)/reports/components/BalesPerDayChart.tsx
  - src/app/api/reports/export/route.ts
  - scripts/e2e-phase4-analysis.sh
autonomous: true

must_haves:
  truths:
    - "An operator is blocked from /reports (redirected away / not shown the analysis page)"
    - "Produksjonsleder sees a date-range analysis page with a from/to date filter"
    - "The page shows an OEE trend over the selected period (per-shift points, day vs evening distinguishable)"
    - "The page shows a downtime Pareto: bars = total stop minutes per reason, line = cumulative %"
    - "The page shows bales per fraction per day (stacked bars) and a day-vs-evening comparison"
    - "A CSV export download is available; the file uses semicolon separators and a UTF-8 BOM"
  artifacts:
    - path: "src/app/(app)/reports/page.tsx"
      provides: "Analysis page (produksjonsleder+), searchParams date range, role gate"
      contains: "getParetoData"
    - path: "src/app/(app)/reports/components/ParetoChart.tsx"
      provides: "Recharts ComposedChart Pareto (dual YAxis)"
      contains: "ComposedChart"
    - path: "src/app/(app)/reports/components/BalesPerDayChart.tsx"
      provides: "Recharts stacked BarChart bales/fraction/day"
      contains: "stackId"
    - path: "src/app/api/reports/export/route.ts"
      provides: "CSV export route handler (semicolon + BOM)"
      contains: "text/csv"
  key_links:
    - from: "src/app/(app)/reports/page.tsx"
      to: "getAnalysis DAL accessors"
      via: "Promise.all over getShiftReportList/getParetoData/getBalesPerDayData/getDayVsEveningComparison with awaited searchParams"
      pattern: "getDayVsEveningComparison\\("
    - from: "src/app/(app)/reports/page.tsx"
      to: "operator role gate"
      via: "redirect when role === 'operator'"
      pattern: "redirect\\("
    - from: "src/app/api/reports/export/route.ts"
      to: "verifySession + getShiftReportList"
      via: "cookies() auth then build CSV"
      pattern: "verifySession\\(\\)"
---

<objective>
Build the date-range analysis page /reports (produksjonsleder+) with an OEE trend, downtime Pareto,
bales-per-day stacked bars, day-vs-evening comparison, and a CSV export route handler.

Purpose: Delivers RPRT-01 (date-range production totals, uptime, OEE trend), RPRT-02 (downtime
Pareto by duration + count), RPRT-03 (bales per fraction over time), RPRT-04 (CSV export), and
SHFT-03 (day vs evening comparison). Also enforces the operator role gate (RESEARCH Open Q1: the
/reports analysis page is produksjonsleder+; shift reports stay open to all).
Output: One server page, four client/server components, one route handler, one E2E script.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-shift-reports-analysis/04-RESEARCH.md
@.planning/phases/04-shift-reports-analysis/04-01-SUMMARY.md
@src/app/(app)/dashboard/page.tsx
@src/app/(app)/dashboard/components/CurrentDrawChart.tsx
@scripts/e2e-phase3.sh
@src/lib/nav.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart components + date filter form (all 'use client')</name>
  <files>src/app/(app)/reports/components/DateRangeForm.tsx, src/app/(app)/reports/components/OeeTrendChart.tsx, src/app/(app)/reports/components/ParetoChart.tsx, src/app/(app)/reports/components/BalesPerDayChart.tsx</files>
  <action>
Create four components. Charts MUST be 'use client' (Recharts in server components fails — same
rule as Phase 3 CurrentDrawChart). Use `isAnimationActive={false}` everywhere to match Phase 3.
Wrap each chart in `<div className="h-64"><ResponsiveContainer width="100%" height="100%">...`.

(1) DateRangeForm.tsx — can be a plain `<form method="GET">` (server component is fine, no client
    state needed; RESEARCH Pattern 2). Props `{ from: string; to: string }`. Two
    `<input type="date" name="from" defaultValue={from}>` / `name="to"` and a submit button "Hent".
    Norwegian labels "Fra" / "Til". Submitting reloads /reports with ?from=&to=.

(2) OeeTrendChart.tsx — 'use client'. Recharts LineChart. Props
    `{ data: { label: string; oeePct: number; shiftType: string }[] }` (one point per shift,
    chronological). X axis = label (e.g. 'dd.MM Dag' / 'dd.MM Kveld'), Y axis 0–100 unit '%'.
    Single Line dataKey="oeePct". Tooltip + CartesianGrid. (Day vs evening is distinguishable via
    the label text per RESEARCH Open Q3 — keep it simple, one line.)

(3) ParetoChart.tsx — 'use client'. Recharts ComposedChart (RESEARCH Pattern 5). Props
    `{ data: { reason: string; minutes: number; incidentCount: number; cumPct: number }[] }`.
    `<YAxis yAxisId="left" unit=" min">`, `<YAxis yAxisId="right" orientation="right" domain={[0,100]} unit="%">`,
    `<Bar yAxisId="left" dataKey="minutes" fill="#3b82f6" isAnimationActive={false}>`,
    `<Line yAxisId="right" type="monotone" dataKey="cumPct" stroke="#f59e0b" isAnimationActive={false}>`.
    XAxis: `dataKey="reason" angle={-35} textAnchor="end" interval={0} tick={{fontSize:10}}` and
    chart `margin={{ top: 8, right: 40, left: 0, bottom: 80 }}` to prevent label clipping
    (RESEARCH Pitfall 4). Tooltip shows minutes + incidentCount (so Pareto exposes BOTH duration and
    count per RPRT-02).

(4) BalesPerDayChart.tsx — 'use client'. Recharts stacked BarChart (RESEARCH Pattern 6). Props
    `{ data: Record<string, number | string>[]; fractions: string[] }` (wide format: each row a day
    with fraction-name keys). XAxis dataKey="date", YAxis, Legend, Tooltip. Map `fractions.map(f =>
    <Bar key={f} dataKey={f} stackId="a" fill={FRACTION_COLORS[f] ?? '#8884d8'} isAnimationActive={false}/>)`.
    Define a FRACTION_COLORS record (Deink #3b82f6, 'Tetra/emballasjepapp' #10b981, OCC #f59e0b,
    Miks #6b7280) with a fallback.
  </action>
  <verify>npx tsc --noEmit (components compile; each chart file begins with 'use client')</verify>
  <done>Four components created. Three charts have 'use client', use isAnimationActive={false}, Pareto has dual YAxis + rotated labels, bales chart stacks via stackId="a". tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Analysis page /reports with operator role gate + searchParams</name>
  <files>src/app/(app)/reports/page.tsx</files>
  <action>
Create the analysis server component (RESEARCH Pattern 1).

- Signature: `export default async function AnalysisPage({ searchParams }: { searchParams: Promise<{ [key: string]: string | string[] | undefined }> })`.
- `const user = await getCurrentUser()`; if null return null.
- ROLE GATE (RESEARCH Open Q1): if `user.role === 'operator'` → `redirect('/reports/shifts')`
  (operator is blocked from analysis; send them to the shift list they CAN see). Import redirect
  from 'next/navigation'.
- Get plant via getPlants()[0]; empty-state card if none.
- Parse params: `const params = await searchParams;
  const fromStr = typeof params.from === 'string' ? params.from : null;
  const toStr = typeof params.to === 'string' ? params.to : null` (RESEARCH Pitfall 2 — values can
  be string[]). Default range = full 14 days: Oslo today (en-CA format) as `to`, and 14 days earlier
  as `from` (same derivation as Plan 02 Task 1).
- `const [shiftRows, pareto, balesLong, comparison] = await Promise.all([
     getShiftReportList(plant.id, from, to), getParetoData(plant.id, from, to),
     getBalesPerDayData(plant.id, from, to), getDayVsEveningComparison(plant.id, from, to) ])`.
- Build chart-ready data SERVER-SIDE (no Date objects across boundary — RESEARCH anti-pattern):
  - OEE trend: map shiftRows (reverse to chronological asc) → `{ label, oeePct: +(r.oee*100).toFixed(1), shiftType }`
    where label = 'dd.MM ' + (Dag|Kveld) from r.startAt (Intl Oslo).
  - Pareto enrichment in JS (RESEARCH Code Example "enrichPareto"): sort desc by totalSeconds,
    compute running cumPct, map to `{ reason, minutes: Math.round(totalSeconds/60), incidentCount, cumPct }`.
  - Bales-per-day pivot to wide: group balesLong by osloDate; for each day build a row
    `{ date: 'dd.MM' label, [fractionName]: count, ... }`. Build `fractions` = ordered distinct
    fraction names from balesLong (or from the accessor's fractions list). Sort rows by date asc.
- Render, top to bottom:
  - `<h1>` "Analyser — {plant.name}".
  - `<DateRangeForm from={from} to={to} />`.
  - A period-totals summary card: total shifts, total bales (sum shiftRows.totalBales), total stop
    time (sum stopSeconds → min), avg OEE. (RPRT-01 production totals + uptime.)
  - A download link: `<a href={\`/api/reports/export?from=${from}&to=${to}\`}>Last ned CSV</a>` styled
    as a button.
  - `<OeeTrendChart data={...} />` under a "OEE-trend" card.
  - `<ParetoChart data={...} />` under a "Nedetid – Pareto" card.
  - `<BalesPerDayChart data={...} fractions={...} />` under a "Baleproduksjon per fraksjon" card.
  - Day-vs-evening comparison table (SHFT-03): two rows (Dag/Kveld) with avgOee %, avgUptimePct %,
    totalBales, totalStopSeconds→min, shiftCount.
  Use dashboard card Tailwind classes throughout.
  </action>
  <verify>npx tsc --noEmit; with dev server: produksjonsleder (leder@steco-demo.no) GET /reports → 200 contains "Analyser"; operator (operator@steco-demo.no) GET /reports → redirected (no "Analyser" heading / lands on /reports/shifts)</verify>
  <done>/reports renders for produksjonsleder with date form, period totals, CSV link, OEE trend, Pareto, bales-per-day stacked bars, and day-vs-evening table. operator is redirected to /reports/shifts. Default range = 14 days. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: CSV export route handler + analysis E2E</name>
  <files>src/app/api/reports/export/route.ts, scripts/e2e-phase4-analysis.sh</files>
  <action>
(A) src/app/api/reports/export/route.ts (RESEARCH Pattern 7):
  - `export async function GET(request: NextRequest)`.
  - Auth: read session via `const cookie = (await cookies()).get('session')?.value` then
    `const session = await decrypt(cookie)` — OR simpler, reuse the DAL: call `verifySession()`
    (it redirects on failure which is fine for a route handler protected behind the app). To return
    a clean 401 instead of redirect for unauthenticated API callers, decode directly: if no
    session.userId → `return new Response('Unauthorized', { status: 401 })`. Import decrypt from
    '@/lib/session' and cookies from 'next/headers'.
  - ROLE GATE: this CSV is the analysis export → if `session.role === 'operator'`
    return `new Response('Forbidden', { status: 403 })`.
  - Parse `from`/`to` from `request.nextUrl.searchParams`; if missing → 400.
  - Resolve the tenant's first plant the same way pages do is awkward in a route handler; instead
    add a tiny tenant-scoped plant lookup: query plants for session.tenantId, take the first; if
    none → 400. (Do this inline with db + eq(plants.tenantId, session.tenantId), OR import getPlants
    from the DAL — getPlants is tenant-scoped and safe to call here.)
  - `const rows = await getShiftReportList(plant.id, from, to)`.
  - Build CSV: `const SEP = ';'; const BOM = '﻿';`
    header = ['Dato','Skift','OEE %','Tilgjengelighet %','Ytelse %','Oppetid (min)','Planlagt (min)','Stopp (antall)','Stoppetid (min)','Totalt baler'].join(SEP).
    For each row: Dato = 'dd.MM.yyyy' Oslo of startAt; Skift = Dag|Kveld; numbers formatted with
    `.toFixed(1)` for percentages, `Math.round(x/60)` for minute fields. Join cells with SEP, rows
    with '\n'. `const csv = BOM + header + '\n' + body`.
  - Return `new Response(csv, { status: 200, headers: { 'Content-Type': 'text/csv; charset=utf-8',
    'Content-Disposition': \`attachment; filename="produksjonsrapport_${from}_${to}.csv"\` } })`.

(B) scripts/e2e-phase4-analysis.sh (model on e2e-phase3.sh):
  - SC0: /login 200.
  - SC1: produksjonsleder leder@steco-demo.no logs in.
  - SC2: GET /reports → 200, HTML contains "Analyser".
  - SC3: HTML contains chart/section markers: "Pareto" and "OEE-trend" and "per fraksjon".
  - SC4: HTML contains a CSV link: grep for `/api/reports/export`.
  - SC5: CSV download — `curl -s -b jar "http://localhost:3000/api/reports/export?from=<from>&to=<to>"`
    using the same default 14-day range; assert response Content starts with a BOM and contains the
    header "Dato;Skift;OEE %". Assert the byte sequence: pipe through `head -c 3 | xxd` and check for
    `efbb bf` (UTF-8 BOM), AND grep the body for ';' separators (e.g. grep -q 'Dato;Skift').
    Also assert Content-Type via `curl -s -D - -o /dev/null .../export?... | grep -qi 'text/csv'`.
  - SC6: operator role gate — login operator@steco-demo.no; GET /reports and assert it does NOT
    contain "Analyser" heading (operator redirected to /reports/shifts). Also GET the export endpoint
    as operator and assert HTTP 403.
  - Print RESULT line, exit non-zero on fail. chmod +x.
  </action>
  <verify>npx tsc --noEmit; with dev server: ./scripts/e2e-phase4-analysis.sh exits 0 — including the BOM byte check (efbb bf), semicolon separator, text/csv content-type, and operator 403 on export</verify>
  <done>CSV export returns semicolon-delimited UTF-8-BOM CSV with the Norwegian header for produksjonsleder; operator gets 403. E2E green incl. BOM + separator + content-type assertions.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit clean.
- With dev server + simulator: ./scripts/e2e-phase4-analysis.sh exits 0.
- CSV: `curl ... /api/reports/export?from=&to= | head -c 3 | xxd` shows `efbb bf`; body contains
  `Dato;Skift;OEE %`.
- Operator GET /reports redirects (no "Analyser"); operator GET /api/reports/export → 403.
- Charts: page HTML contains "Pareto", "OEE-trend", "per fraksjon" section labels.
</verification>

<success_criteria>
/reports shows date-range analysis (OEE trend, downtime Pareto with duration+count, bales-per-day
stacked, day-vs-evening table, period totals) for produksjonsleder+. Operator is blocked from both
the page and the CSV endpoint. CSV export downloads with semicolon separators and UTF-8 BOM. E2E green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shift-reports-analysis/04-03-SUMMARY.md`
</output>
