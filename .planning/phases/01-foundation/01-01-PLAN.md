---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - .env.local
  - .env.example
  - drizzle.config.ts
  - src/db/index.ts
autonomous: true

must_haves:
  truths:
    - "Project dependencies install cleanly using the project-local npm cache"
    - "A SQLite database connection is available to the rest of the app via a single import"
    - "drizzle-kit is configured and can read the schema location"
    - "Secrets and generated artifacts (db file, npm cache) are git-ignored"
  artifacts:
    - path: "src/db/index.ts"
      provides: "Drizzle db singleton over better-sqlite3 (HMR-safe)"
      contains: "globalForDb"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit configuration pointing at src/db/schema.ts"
      contains: "dialect: 'sqlite'"
    - path: ".env.local"
      provides: "DB_FILE_NAME and SESSION_SECRET for local dev"
      contains: "SESSION_SECRET"
    - path: ".gitignore"
      provides: "Ignore rules for .npm-cache and the db file"
      contains: ".npm-cache"
  key_links:
    - from: "src/db/index.ts"
      to: "process.env.DB_FILE_NAME"
      via: "new Database(process.env.DB_FILE_NAME)"
      pattern: "DB_FILE_NAME"
    - from: "drizzle.config.ts"
      to: "src/db/schema.ts"
      via: "schema path config"
      pattern: "schema.*src/db/schema"
---

<objective>
Establish the project foundation: install all Phase 1 dependencies, configure environment variables and git ignores, and create the HMR-safe SQLite database connection plus drizzle-kit configuration. This is the root of the dependency graph — schema, sessions, and seed all depend on the db connection and tooling created here.

Purpose: Everything else in Phase 1 needs a working db connection, installed libraries, and migration tooling. Getting the npm-cache, env, and HMR-singleton details right here prevents the pitfalls documented in research (EACCES cache errors, SQLITE_BUSY on hot reload).
Output: Installed dependencies, `src/db/index.ts` (db singleton), `drizzle.config.ts`, `.env.local`, `.env.example`, updated `.gitignore`.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

# The repo already contains a create-next-app scaffold (Next.js 16.2.9, React 19, Tailwind v4, TypeScript, @/* path alias). node_modules is NOT yet installed. .npm-cache/ exists but is NOT yet gitignored.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies with project-local npm cache</name>
  <files>package.json, package-lock.json</files>
  <action>
First install the existing base dependencies (node_modules is currently missing), then add the Phase 1 libraries. ALWAYS use the project-local cache flag — the global npm cache has EACCES permission problems in this sandbox (see research Pitfall 5).

Run exactly:
```
npm install --cache .npm-cache
npm install --cache .npm-cache jose@6.2.3 bcryptjs@3.0.3 zod@4.4.3 server-only drizzle-orm@0.45.2 better-sqlite3@12.10.0
npm install --cache .npm-cache -D drizzle-kit@0.31.10 @types/better-sqlite3 dotenv tsx
```

Notes:
- `tsx` is included as a dev dependency so the seed script (Plan 02) can run via `npx tsx`.
- Do NOT install next-auth — research Pitfall 1: v5 is still beta as of June 2026; this project uses hand-rolled jose sessions.
- If any install reports EACCES on _cacache, confirm the `--cache .npm-cache` flag is present and retry.
  </action>
  <verify>
`ls node_modules/jose node_modules/drizzle-orm node_modules/better-sqlite3 node_modules/bcryptjs node_modules/zod node_modules/drizzle-kit` all resolve without error. `node -e "require('better-sqlite3')"` exits 0 (native addon built).
  </verify>
  <done>All listed packages present in node_modules; package.json dependencies include jose, bcryptjs, zod, server-only, drizzle-orm, better-sqlite3, and devDependencies include drizzle-kit, @types/better-sqlite3, dotenv, tsx. better-sqlite3 loads without a native build error.</done>
</task>

<task type="auto">
  <name>Task 2: Configure env files and git ignores</name>
  <files>.env.local, .env.example, .gitignore</files>
  <action>
Create `.env.local` with:
```
DB_FILE_NAME=./stecopro.db
SESSION_SECRET=dev-only-change-me-32-bytes-min-secret-value
```
Generate a real random value for SESSION_SECRET (e.g. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`) and use that instead of the placeholder.

Create `.env.example` (committed, no real secret) with:
```
DB_FILE_NAME=./stecopro.db
SESSION_SECRET=replace-with-32-byte-random-hex
```

Update `.gitignore`: the existing file already ignores `.env*` and `/node_modules`. Append a section so the project-local npm cache and the SQLite db file are ignored. Add these lines (only if not already present):
```
# project-local npm cache (sandbox workaround)
.npm-cache/

# sqlite demo database
*.db
*.db-journal
*.db-wal
*.db-shm
```
Note: `.env*` is already ignored, but `.env.example` must be committable. Add a negation line `!.env.example` so the example template is tracked.
  </action>
  <verify>
`git check-ignore .npm-cache/ stecopro.db .env.local` lists all three as ignored. `git check-ignore .env.example` produces NO output (i.e. it is NOT ignored — it is committable). `cat .env.local` shows a non-placeholder 64-char hex SESSION_SECRET.
  </verify>
  <done>.env.local has a real random SESSION_SECRET and DB_FILE_NAME; .env.example is committable; .npm-cache/, *.db, and .env.local are gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Create db connection singleton and drizzle config</name>
  <files>src/db/index.ts, drizzle.config.ts</files>
  <action>
Create `src/db/index.ts` using the HMR-safe global singleton pattern (research Pitfall 6 — prevents SQLITE_BUSY on Next.js hot reload):
```typescript
import 'server-only'
import Database from 'better-sqlite3'
import { drizzle } from 'drizzle-orm/better-sqlite3'
import * as schema from './schema'

const globalForDb = globalThis as unknown as { db?: ReturnType<typeof drizzle<typeof schema>> }

export const db =
  globalForDb.db ?? drizzle(new Database(process.env.DB_FILE_NAME ?? './stecopro.db'), { schema })

if (process.env.NODE_ENV !== 'production') globalForDb.db = db
```
This imports `./schema` (created in Plan 02). The import will type-error until schema.ts exists — that is expected; Plan 02 creates it. Do NOT create a stub schema.ts here (Plan 02 owns that file).

Create `drizzle.config.ts` at project root:
```typescript
import type { Config } from 'drizzle-kit'

export default {
  schema: './src/db/schema.ts',
  out: './drizzle',
  dialect: 'sqlite',
  dbCredentials: {
    url: process.env.DB_FILE_NAME ?? './stecopro.db',
  },
} satisfies Config
```
  </action>
  <verify>
`src/db/index.ts` exports `db` and uses `globalForDb` + `process.env.DB_FILE_NAME`. `drizzle.config.ts` exists with `dialect: 'sqlite'` and `schema: './src/db/schema.ts'`. (TypeScript will report a missing './schema' module — acceptable, resolved in Plan 02.)
  </verify>
  <done>db singleton and drizzle.config.ts created exactly as above; both reference DB_FILE_NAME; schema import points at the file Plan 02 will create.</done>
</task>

</tasks>

<verification>
- `npm run dev` is not started here, but `node -e "require('better-sqlite3')"` confirms the native driver works.
- `git check-ignore` confirms cache, db, and secret are ignored while .env.example is tracked.
- The db singleton and drizzle config exist and reference the same DB_FILE_NAME env var.
</verification>

<success_criteria>
- All Phase 1 npm dependencies installed via project-local cache.
- A single `import { db } from '@/db'` gives the rest of the app a working SQLite connection.
- drizzle-kit can locate the (forthcoming) schema and the db file.
- No secrets, db files, or the npm cache will be committed; .env.example template is committable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
