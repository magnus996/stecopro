---
phase: 05-administration-demo-polish
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-03", "05-04", "05-05"]
files_modified:
  - scripts/e2e-phase5-walkthrough.sh
autonomous: true

must_haves:
  truths:
    - "A single E2E script logs in as each of the four roles and walks the full demo path"
    - "Operator: reaches dashboard + shift report; is blocked from /reports, /admin/plant, /admin/users, /admin/tenants"
    - "Produksjonsleder: reaches analysis + plant config; is blocked from /admin/users and /admin/tenants"
    - "Admin: reaches user management; is blocked from /admin/tenants"
    - "System admin: reaches tenant management and sees both tenants"
    - "User CRUD round-trips: a newly created user can log in; a deactivated user is rejected (401)"
    - "The script exits 0 when all assertions pass"
  artifacts:
    - path: "scripts/e2e-phase5-walkthrough.sh"
      provides: "curl-based full role-walkthrough + role-gate + user CRUD E2E"
      contains: "RESULT:"
  key_links:
    - from: "scripts/e2e-phase5-walkthrough.sh"
      to: "/api/auth/login"
      via: "per-role cookie jars, asserts {ok:true}"
      pattern: "api/auth/login"
    - from: "scripts/e2e-phase5-walkthrough.sh"
      to: "role gates"
      via: "asserts redirect/403 for forbidden role+route combinations"
      pattern: "admin/tenants"
---

<objective>
Prove success criterion 5: a full demo walkthrough (login as each role → dashboard → shift report → downtime analysis → admin) works without touching code, and verify the role gates and user-CRUD behaviors end to end.

Purpose: This is the LAST plan of the demo milestone. It is the executable proof that every Phase 5 page exists, every role gate holds, and the user lifecycle (create → login → deactivate → login rejected) works. It also doubles as the demo runbook.

Output: scripts/e2e-phase5-walkthrough.sh following the established curl-E2E pattern (BASE override, pass/fail counter, exit code).
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-administration-demo-polish/05-RESEARCH.md
@scripts/e2e-phase4-shifts.sh
@scripts/e2e-phase3.sh
@src/app/api/auth/login/route.ts
@src/lib/nav.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the full role-walkthrough + role-gate + user-CRUD E2E script</name>
  <files>scripts/e2e-phase5-walkthrough.sh</files>
  <action>
Create scripts/e2e-phase5-walkthrough.sh (#!/bin/zsh, chmod +x). Mirror the structure of e2e-phase4-shifts.sh exactly: `BASE=${BASE:-http://localhost:3000}`, a `pass`/`fail` counter, a `check()` helper, and a `login()` helper that POSTs JSON to `$BASE/api/auth/login` with a per-role cookie jar (/tmp/c_ph5_<role>.txt) and `password:demo123`. The login route returns `{"ok":true,"role":"..."}`.

Add a small helper to fetch an HTTP status for a path with a given cookie jar:
`status() { curl -s -o /dev/null -w '%{http_code}' -b "$2" "$BASE$1"; }`
and a body fetch:
`body() { curl -s -b "$2" "$BASE$1"; }`
Note: forbidden routes redirect via `redirect('/dashboard')` (307) at the page level, but proxy may also intervene. Treat "blocked" as: status is NOT 200 OR the body does NOT contain the page's signature heading. Provide a helper `blocked()` that passes if `status != 200` OR body lacks the expected marker.

SC0 — app up: GET /login == 200.

Log in all four roles into separate cookie jars and assert `{ok:true}`:
- operator@steco-demo.no, leder@steco-demo.no, admin@steco-demo.no, system@steco.no.

OPERATOR walkthrough:
- /dashboard == 200 (contains a known dashboard marker, e.g. 'OEE' or 'Dashbord' — check what the dashboard page renders; use a stable string).
- /reports/shifts == 200 and contains 'Skiftrapporter'.
- BLOCKED: /reports (analysis) — operator redirected to /reports/shifts; assert NOT containing the analysis-only marker 'Analyser —' OR status != 200.
- BLOCKED: /admin/plant, /admin/users, /admin/tenants — each blocked() (no 'Anleggsoppsett' / 'Brukere' / 'Tenants' heading).

PRODUKSJONSLEDER walkthrough:
- /dashboard == 200; /reports/shifts == 200.
- /reports == 200 containing 'Analyser' (analysis page reachable).
- CSV export: /api/reports/export?from=...&to=... — just assert it returns 200 with a recent default range (reuse the same default the page uses; or GET without params if the route defaults). If the route requires params, derive a 14-day range in the script (date math via `date -v-14d +%F` on macOS/zsh).
- /admin/plant == 200 containing 'Anleggsoppsett' and 'Skifttider'.
- BLOCKED: /admin/users, /admin/tenants.

ADMIN walkthrough:
- /admin/plant == 200; /admin/users == 200 containing 'Brukere'.
- BLOCKED: /admin/tenants.

SYSTEM_ADMIN walkthrough:
- /admin/users == 200; /admin/tenants == 200 containing 'Tenants' AND both 'Steco Demo' AND 'Isolasjonstest' (proves cross-tenant visibility + ADMN-04 isolation seed).

USER CRUD round-trip (uses admin cookie jar):
- Create a fresh user via the server action. Server actions are not directly curl-able as JSON, so create the user by POSTing to the same DB through a tiny tsx assertion is NOT ideal. INSTEAD verify CRUD at the DB layer to keep the script self-contained: use `npx tsx -e` to (a) confirm a known seeded user count, then the script focuses on the OBSERVABLE login behaviors:
  - DEACTIVATED-USER-CANNOT-LOGIN: pick or create a disposable user. Simplest deterministic approach: via `npx tsx -e`, insert a user `e2e-temp@steco-demo.no` (bcrypt hash of 'demo123' — import bcryptjs) active=true in tenant 'steco-demo'; assert login API returns {ok:true}. Then set active=false via tsx; assert login API now returns HTTP 401 (deactivated cannot log in). Finally delete the temp user via tsx for idempotency.
  - This exercises the exact auth.ts `!user.active` path that the deactivateUser action relies on, proving the must_have "deactivated user cannot log in" without depending on RSC server-action invocation over curl.
(Keep the tsx snippets inline in the script, using `process.env.DB_FILE_NAME ?? './stecopro.db'` and better-sqlite3 directly, matching the seed.ts connection style.)

End with the standard footer:
`echo ""; echo "RESULT: $pass passed, $fail failed"; [ $fail -eq 0 ]`

Make the script executable (`chmod +x scripts/e2e-phase5-walkthrough.sh`).
  </action>
  <verify>
Ensure the demo DB is freshly built (`npm run demo:setup` from Plan 01 already ran; re-run if needed) and the dev server is running. Then run `./scripts/e2e-phase5-walkthrough.sh` (override BASE if the dev server is on another port, e.g. `BASE=http://localhost:3000`). It must print "RESULT: N passed, 0 failed" and exit 0.
  </verify>
  <done>scripts/e2e-phase5-walkthrough.sh runs green: all four role walkthroughs pass, every forbidden role+route is blocked, system_admin sees both tenants, and the deactivated-user login is rejected (401). Temp user cleaned up.</done>
</task>

</tasks>

<verification>
- `./scripts/e2e-phase5-walkthrough.sh` exits 0 with 0 failures against the running dev server + freshly seeded DB.
- Operator blocked from analysis + all admin routes; produksjonsleder blocked from users + tenants; admin blocked from tenants; system_admin reaches tenants and sees both tenants.
- A user toggled inactive in the DB is rejected by /api/auth/login (401).
</verification>

<success_criteria>
Success criterion 5 satisfied: a full role walkthrough (login per role → dashboard → shift report → analysis → admin) and all role gates are proven by a single green E2E script that doubles as the demo runbook.
</success_criteria>

<output>
After completion, create `.planning/phases/05-administration-demo-polish/05-06-SUMMARY.md`
</output>
