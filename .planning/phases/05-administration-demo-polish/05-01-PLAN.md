---
phase: 05-administration-demo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/seed.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Seed creates tenant 'Steco Demo' (returpapir plant) AND a second tenant 'Isolasjonstest'"
    - "Tenant 2 has its own plant, fractions, machines, and ~1 week of shift/bale/stop data so its dashboard shows real (different) data"
    - "Logging in as bruker@isolasjonstest.no shows a populated dashboard scoped only to tenant 2"
    - "A single command (npm run demo:setup) rebuilds the entire demo from scratch"
  artifacts:
    - path: "src/db/seed.ts"
      provides: "Tenant 2 plant + fractions + machines + static shift/bale/stop/reading rows"
      contains: "Test Linje 1"
    - path: "package.json"
      provides: "demo:setup script"
      contains: "demo:setup"
  key_links:
    - from: "src/db/seed.ts"
      to: "tenant2 plant rows"
      via: "static shift + baleEvents + stopEvents inserts scoped to tenant2.id"
      pattern: "tenant2\\.id"
---

<objective>
Polish the demo seed so tenant isolation is visually provable and the whole demo starts from one command (ADMN-04).

Purpose: Success criterion 4 requires "Steco Demo" with the returpapir plant PLUS a second tenant proving isolation. Today tenant 2 has one user and no data — logging in shows an empty "Ingen anlegg" screen, which does not convincingly prove isolation. This plan gives tenant 2 a minimal plant with a week of static data so its dashboard renders real, different numbers.

Output: Updated src/db/seed.ts with tenant-2 plant/fractions/machines + static shift/bale/stop/reading rows, and a demo:setup npm script.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-administration-demo-polish/05-RESEARCH.md
@src/db/seed.ts
@src/db/schema.ts
@scripts/simulate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed tenant 2 with a minimal plant and static production data</name>
  <files>src/db/seed.ts</files>
  <action>
After the existing tenant2 user insert (around line 119), add a self-contained block that gives tenant 2 a real (but small) plant so its dashboard proves isolation. Use the SAME direct `db` connection already set up in this file (NOT the simulator — scripts/simulate.ts uses `plantRows[0]` and would only simulate tenant 1's plant, so tenant 2 data MUST be seeded statically here).

Import `shifts, stopEvents, baleEvents, timeSeriesReadings` from './schema' (extend the existing import block; tenants/users/plants/fractions/machines already imported).

Insert for tenant2.id:
1. One plant: name 'Test Linje 1', description 'Testanlegg for isolasjonstest', nominalCapacityTph: 8, createdAt: new Date(). Capture as `plant2` via .returning().all().
2. Two fractions: { tenantId: tenant2.id, plantId: plant2.id, name: 'Fraksjon A', sortOrder: 0 } and { ...'Fraksjon B', sortOrder: 1 }. Capture returned rows to get fraction ids.
3. One bunker machine: { tenantId: tenant2.id, plantId: plant2.id, name: 'Bunker T2', type: 'bunker', nominalCurrentA: 30, createdAt: new Date() }. Capture id.
4. Static history for the last 7 Oslo calendar days, two shifts/day (day 07–15, evening 15–22). Generate shift rows + a handful of bale/stop/reading rows per shift WITHOUT importing simulator code. Keep it deterministic and simple:
   - For each of the 7 days and each shift, compute startAt/endAt in UTC. Reuse `getShiftBoundsUtc(osloDateStr, shiftType)` from '@/lib/time' (import it). osloDateStr derived via `new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/Oslo' }).format(date)` for each day going back from today; skip the current day's shifts that have not started yet (startAt > now).
   - Insert a `shifts` row per generated shift (shiftType 'day'|'evening').
   - Insert ~6 bale rows per shift spread across the shift window, alternating fractionA/fractionB ids, occurredAt within [startAt, endAt), weightKg ~500.
   - Insert 1–2 closed `stopEvents` per shift: startAt offset into the shift, endAt = startAt + (5–8 min), reason e.g. 'driftsstans transportbånd' (stopType 'fault') and optionally 'Bunker tom' (stopType 'idle'). Keep total stop time small so availability stays high.
   - Insert ~8 `timeSeriesReadings` rows per shift for the bunker machine spread across the window: recordedAt within shift, currentA around 25–32 (running) with one or two dips toward ~2 (idle), runState true/false accordingly. This feeds the dashboard current-draw card and "no_data" freshness logic for historical view.

   IMPORTANT: do NOT generate a "live now" reading for tenant 2 (the simulator only keeps tenant 1 live). The point is to prove isolation in lists/reports, not live ticking. Outside-shift / stale dashboard state for tenant 2 is acceptable and expected.

Update the closing `counts` summary log to also count shifts/baleEvents/stopEvents so the seed output confirms tenant-2 data was written. Keep the file idempotent — the existing delete-all-rows cleanup at the top already covers the new tables (timeSeriesReadings, baleEvents, stopEvents, shifts are all deleted there).
  </action>
  <verify>
Run: `DB_FILE_NAME=./stecopro-seedtest.db npm run db:seed` then a quick assertion via tsx that tenant 2 has a plant and >0 shifts/bales. Example:
`DB_FILE_NAME=./stecopro-seedtest.db npx tsx -e "import Database from 'better-sqlite3'; const d=new Database('./stecopro-seedtest.db'); const t=d.prepare(\"select id,name from tenants where slug='isolasjonstest'\").get(); const p=d.prepare('select count(*) c from plants where tenant_id=?').get(t.id); const s=d.prepare('select count(*) c from shifts where tenant_id=?').get(t.id); const b=d.prepare('select count(*) c from bale_events where tenant_id=?').get(t.id); console.log('tenant2',t.id,'plants',p.c,'shifts',s.c,'bales',b.c); if(p.c<1||s.c<1||b.c<1) process.exit(1)"`
Then remove the test DB: `rm -f ./stecopro-seedtest.db`. Seed of the real DB happens in Task 2 verify.
  </verify>
  <done>Seed inserts tenant 2 plant + 2 fractions + 1 machine + >=1 week of shift/bale/stop/reading rows; assertion prints plants>=1, shifts>=1, bales>=1; test DB removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add demo:setup one-command script and rebuild the demo DB</name>
  <files>package.json</files>
  <action>
Add a `demo:setup` script to package.json scripts that runs the full demo build from scratch. It must seed (which now includes tenant 2 static data) and then run the tenant-1 live simulator backfill. Define:
`"demo:setup": "npm run db:seed && npm run db:simulate"`
(This is the same chain as the existing `db:reset`; keep `db:reset` too — `demo:setup` is the salesperson-facing alias documented in the walkthrough guide.)

Then actually rebuild the real demo database by running `npm run demo:setup` so the running app has tenant-2 data for the later walkthrough E2E. (The simulate step backfills 14 days for tenant 1 — unchanged behavior.)
  </action>
  <verify>
`npm run demo:setup` exits 0. Then assert both tenants have data in the real DB:
`npx tsx -e "import Database from 'better-sqlite3'; const d=new Database(process.env.DB_FILE_NAME ?? './stecopro.db'); const rows=d.prepare('select t.slug, (select count(*) from plants p where p.tenant_id=t.id) plants, (select count(*) from shifts s where s.tenant_id=t.id) shifts from tenants t').all(); console.log(rows); const iso=rows.find(r=>r.slug==='isolasjonstest'); if(!iso||iso.plants<1||iso.shifts<1) process.exit(1)"`
  </verify>
  <done>`npm run demo:setup` rebuilds the DB; both 'steco-demo' and 'isolasjonstest' tenants report plants>=1 and shifts>=1.</done>
</task>

</tasks>

<verification>
- `npm run demo:setup` completes without error and is a single command.
- Real DB: tenant 'isolasjonstest' has its own plant and shift/bale data (isolation is now visually provable on its dashboard).
- Tenant 1 (Steco Demo) data unchanged — simulator still backfills/lives only tenant 1.
</verification>

<success_criteria>
ADMN-04 satisfied: seed produces "Steco Demo" with the returpapir plant plus a second tenant with its own minimal plant + data proving isolation; `npm run demo:setup` rebuilds the demo from scratch in one command.
</success_criteria>

<output>
After completion, create `.planning/phases/05-administration-demo-polish/05-01-SUMMARY.md`
</output>
