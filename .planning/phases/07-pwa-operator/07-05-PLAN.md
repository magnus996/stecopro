---
phase: 07-pwa-operator
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - src/app/(app)/skift/page.tsx
  - src/app/(app)/varsler/page.tsx
  - src/app/(app)/stopp/[id]/page.tsx
  - src/components/StopActions.tsx
  - src/components/ShiftNoteComposer.tsx
  - src/lib/nav.ts
  - src/proxy.ts
  - scripts/e2e-phase7.sh
  - README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "/skift, /varsler and /stopp/[id] return 200 for a logged-in operator and contain the expected Norwegian markers"
    - "Nav shows «Mitt skift» and «Varsler» for all roles; proxy protects /skift, /varsler, /stopp"
    - "/varsler lists recent notifiable stops with ack status and an enable-notifications toggle, and works without push permission"
    - "/skift shows today's stops with acknowledge + comment + camera and a shift-notes logbook"
    - "e2e-phase7.sh proves the full flow trigger→ack→comment→photo→note plus 401/403 and tenant-isolation negatives"
    - "npm test and demo:setup stay green; phase 1/3/5/6 E2E suites still pass"
  artifacts:
    - path: "src/app/(app)/skift/page.tsx"
      provides: "«Mitt skift» operator home: status, today's stops with actions, shift log"
      contains: "Mitt skift"
    - path: "src/app/(app)/varsler/page.tsx"
      provides: "Notification history (48h) + enable-notifications toggle"
      contains: "Varsler"
    - path: "src/app/(app)/stopp/[id]/page.tsx"
      provides: "Stop detail: info, acks, comment thread with photos, correction form"
      contains: "notFound"
    - path: "scripts/e2e-phase7.sh"
      provides: "Full-flow curl E2E incl. negatives"
      contains: "trigger-stop"
    - path: "src/lib/nav.ts"
      provides: "Mitt skift + Varsler nav entries for all roles"
      contains: "/skift"
  key_links:
    - from: "src/proxy.ts protectedRoutes"
      to: "/skift, /varsler, /stopp"
      via: "array entries"
      pattern: "/skift"
    - from: "src/app/(app)/varsler/page.tsx"
      to: "PushToggle + getRecentNotifiableStops"
      via: "import + render"
      pattern: "PushToggle"
    - from: "src/app/(app)/stopp/[id]/page.tsx"
      to: "getStopDetail + StopActions"
      via: "DAL read + client actions"
      pattern: "getStopDetail"
---

<objective>
Ship the operator-facing UI (/skift, /varsler, /stopp/[id]), wire nav + proxy, and prove the whole Phase 7 flow end-to-end with scripts/e2e-phase7.sh, plus demo docs.

Purpose: PWAS-01/NOTI-02 surfaces + the REPT-01..04 operator interactions, tying together everything from 07-01..07-04. Wave 3 — runs after the push pipeline (07-03), reporting APIs (07-04) and PWA shell (07-02) exist.
Output: 3 pages, 2 client components (StopActions, ShiftNoteComposer), nav + proxy edits, e2e-phase7.sh, README demo section.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pwa-operator/07-RESEARCH.md
@src/app/(app)/layout.tsx
@src/lib/nav.ts
@src/proxy.ts
@src/lib/dal.ts
@scripts/e2e-phase5-walkthrough.sh
@src/components/PushToggle.tsx
@src/app/(app)/inventory/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pages + client action components</name>
  <files>src/app/(app)/skift/page.tsx, src/app/(app)/varsler/page.tsx, src/app/(app)/stopp/[id]/page.tsx, src/components/StopActions.tsx, src/components/ShiftNoteComposer.tsx</files>
  <action>
Mobile-first, responsive — NO separate /mobile tree (research arch #8). Server Components fetch via the 07-04 DAL accessors; interactivity lives in small 'use client' leaves that POST to the 07-04 route handlers. Match the existing app's Tailwind card style (look at inventory/page.tsx and dashboard for conventions: zinc palette, dark mode classes, card shadows). All copy in Norwegian.

src/app/(app)/skift/page.tsx — «Mitt skift» (manifest start_url; REPT-01/02/03 entry):
- Server component. Resolve the tenant's plant (reuse getPlants() or a dashboard accessor). Heading must literally contain «Mitt skift».
- Sections: (1) plant status line; (2) «Dagens stopp» — render getTodaysStopsWithAcks(plantId); each stop links to /stopp/{id} and shows reason, time (HH:mm Oslo), ack count, and an inline StopActions (Kvitter / kommentar / kamera) leaf; (3) «Skiftlogg» — render getShiftNotes(plantId) plus a ShiftNoteComposer at the top.
- Serialize timestamps to 'HH:mm' Oslo strings in the page BEFORE crossing the server→client boundary (project convention from Phase 3).

src/app/(app)/varsler/page.tsx — «Varsler» (NOTI-02):
- Server component. Heading contains «Varsler». Render <PushToggle /> (the 07-03 client component) labelled «Aktiver varsler på denne enheten» — the page MUST work without push permission (the toggle degrades; the list still renders).
- Render getRecentNotifiableStops(plantId): last 48h fault + Bunker-tom stops, each with reason, time, kvitteringsstatus (e.g. «Kvittert» vs «Ikke kvittert» based on ackCount), linking to /stopp/{id}.

src/app/(app)/stopp/[id]/page.tsx — stop detail (deep-link target; REPT-01/02/04):
- Server component, `params: Promise<{id:string}>`, `await params`. Call getStopDetail(Number(id)); if null → `notFound()` (import from next/navigation).
- Show stop info (reason, type, start/end, duration), the acknowledgement list (names + time), the comment thread (each comment shows author, text, corrected reason if any, and if photoId an `<img src={`/api/photos/${photoId}`}>`), and a StopActions block for kvittering + kommentar (with korrigeringsskjema: a select/input to set correctedReason) + kamera upload.

src/components/StopActions.tsx — 'use client', props { stopId }:
- «Kvitter» button → POST /api/stops/{stopId}/ack, then router.refresh().
- Comment form: textarea (comment) + optional correctedReason input + a file input `<input type="file" accept="image/*" capture="environment">` (research: this opens the mobile camera directly). On submit: if a file is chosen, first POST it as multipart to /api/photos → get {id}, then POST /api/stops/{stopId}/comments with { comment, correctedReason, photoId }. router.refresh() after. Show validation: require comment or corrected reason (mirror the API rule) to avoid a 400 round-trip.
- Keep it compact and reusable (used both inline on /skift and on /stopp/[id]).

src/components/ShiftNoteComposer.tsx — 'use client', props { } (plant resolved server-side or note endpoint defaults to first plant):
- textarea (content) + optional camera file input. On submit: optional photo upload → POST /api/notes { content, photoId }. router.refresh().

Use `useRouter().refresh()` (next/navigation) for revalidation after writes — same lightweight pattern as the inventory ShipmentForm. Do not introduce new libraries.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -iE "skift|varsler|stopp|StopActions|ShiftNoteComposer" || echo "pages typecheck". Dev server on a unique port (then stop): login as operator → GET /skift (200, body contains «Mitt skift»), /varsler (200, contains «Varsler»), and /stopp/{an existing stop id} (200). A non-existent stop id → 404.</verify>
  <done>Three pages render 200 for a logged-in operator with the Norwegian markers; /stopp uses notFound() for missing/foreign stops; StopActions and ShiftNoteComposer POST to the 07-04 endpoints and refresh; camera input present on photo controls.</done>
</task>

<task type="auto">
  <name>Task 2: Nav + proxy wiring</name>
  <files>src/lib/nav.ts, src/proxy.ts</files>
  <action>
src/lib/nav.ts: add two NAV_ITEMS for ALL roles (operator..system_admin), placed near the top (operator's primary surface). Mirror the existing entry shape:
```
{ label: 'Mitt skift', href: '/skift',   roles: ['operator', 'produksjonsleder', 'admin', 'system_admin'] },
{ label: 'Varsler',    href: '/varsler', roles: ['operator', 'produksjonsleder', 'admin', 'system_admin'] },
```
(Do NOT add /stopp to nav — it's a deep-link target, not a top-level destination.)

src/proxy.ts: extend `protectedRoutes` to include the three new prefixes:
```
const protectedRoutes = ['/dashboard', '/reports', '/admin', '/skift', '/varsler', '/stopp']
```
Leave the matcher and the api/static exclusions as-is (the matcher already excludes /api and *.png, which is correct: /api/photos etc. handle their own auth via getApiSession, and the manifest/icon/sw routes must stay reachable). Confirm /sw.js and /manifest.webmanifest are NOT blocked by the proxy (they aren't, since the matcher excludes _next and the public file sw.js is served as a static asset / the manifest route is under the app but not a protected prefix — verify /manifest.webmanifest and /sw.js still return 200 to an unauthenticated client).
  </action>
  <verify>grep -q "'/skift'" src/proxy.ts && grep -q "'/varsler'" src/proxy.ts && grep -q "'/stopp'" src/proxy.ts && echo PROXY_OK; grep -q "Mitt skift" src/lib/nav.ts && grep -q "Varsler" src/lib/nav.ts && echo NAV_OK. Dev server on a unique port: unauthenticated GET /skift → redirect to /login (307/302); unauthenticated GET /manifest.webmanifest → 200; unauthenticated GET /sw.js → 200. Stop the server.</verify>
  <done>Nav exposes «Mitt skift» + «Varsler» for all roles; proxy protects /skift,/varsler,/stopp (unauth → /login) while manifest and sw.js remain publicly reachable.</done>
</task>

<task type="auto">
  <name>Task 3: e2e-phase7.sh full-flow script + demo docs</name>
  <files>scripts/e2e-phase7.sh, README.md</files>
  <action>
Create scripts/e2e-phase7.sh (zsh, executable), following the structure of scripts/e2e-phase5-walkthrough.sh exactly: `BASE=${BASE:-http://localhost:3000}`, `pass/fail` counters, `check()`, `login()` (POST JSON to /api/auth/login writing a cookie jar), `status()`, `body()`. End with `echo "RESULT: $pass passed, $fail failed"` and `[ $fail -eq 0 ]`.

Cover (Norwegian check labels), in order:
- SC0: /login returns 200 (app up).
- SC1: operator login (operator@steco-demo.no / demo123) ok; tenant-2 login (bruker@isolasjonstest.no) ok (for the isolation negative).
- SC2 pages: operator GET /skift 200 + body grep «Mitt skift»; /varsler 200 + grep «Varsler».
- SC3 push negatives: POST /api/push/subscribe WITHOUT a cookie → 401; GET /api/push/vapid-public-key → 200 (keys present) [if 503, still pass as a documented degradation — assert it's 200 OR 503].
- SC4 trigger→notify: POST /api/dev/trigger-stop with the operator jar → 200; capture the JSON; assert it contains `"stopId"` and `"attempted"`; extract `stopId` (grep/sed/python3 -c json). (Subscribe a fake push subscription first via /api/push/subscribe so attempted>=1, then assert attempted>=1; clean it up at the end.)
- SC5 ack idempotency: POST /api/stops/{stopId}/ack twice → both ok; (optional) assert one row via inline tsx select count.
- SC6 comment + correction: POST /api/stops/{stopId}/comments with {} → 400; with {comment:'sjekket på stedet'} → 201; with {correctedReason:'papirbrudd i presse'} → 201.
- SC7 photo round-trip: write a 1x1 PNG to a tmp file (base64 decode an inline string), POST multipart to /api/photos → 201, extract id; GET /api/photos/{id} with jar → 200 image/png; POST a >10MB file (create with `dd`/`head -c 11000000`) labelled image → 400; tenant-2 jar GET /api/photos/{tenant1 photo id} → 404 (isolation).
- SC8 note: POST /api/notes {content:'E2E skiftnotat'} → 201; then GET /skift and grep the note text appears in the logbook.
- SC9 stop detail page: GET /stopp/{stopId} (operator jar) → 200; GET /stopp/999999 → 404 (or non-200).
- SC10 deep-link visibility: GET /skift body shows the triggered stop's reason; comment thread visible on /stopp/{stopId} (grep 'sjekket på stedet').
- Cleanup: remove the fake push subscription row and tmp files at the end (inline tsx delete by endpoint, mirroring e2e-phase5's tsx snippets).

Use `python3 -c` or grep/sed to parse JSON (no jq dependency assumed; e2e-phase5 used grep). Default BASE to localhost:3000 but support the BASE override env like phase 5.

README.md: add a «Fase 7 — PWA-operatørapp» section documenting (research arch #10 + verification section):
- Desktop demo: `npm run demo:setup && npm run dev`, log in, open /varsler, «Aktiver varsler» (localhost = secure context in Chrome), «Utløs teststans», OS notification → click opens /stopp/{id} → Kvitter + kommenter + last opp bilde.
- Mobile/HTTPS: `next dev --experimental-https` (or tailscale serve / ngrok); iOS needs 16.4+ and «Legg til på Hjem-skjerm» before push; /varsler is the fallback without install.
- VAPID keys: `npx web-push generate-vapid-keys` into .env.local (VAPID_PUBLIC_KEY/PRIVATE_KEY/SUBJECT); missing keys degrade gracefully.
- Run the E2E: `zsh scripts/e2e-phase7.sh` (server must be running + demo seeded).
  </action>
  <verify>chmod +x scripts/e2e-phase7.sh. Start a dev server on a UNIQUE port with seeded data (npm run demo:setup first, then PORT=3075 npm run dev in background), then `BASE=http://localhost:3075 zsh scripts/e2e-phase7.sh` → RESULT line shows 0 failed. Stop the server. Also run `npm test` (green) and the regression suites: e2e-phase1/3/5/6 against the same server before stopping it (BASE override) — all green. Confirm README contains 'Fase 7'.</verify>
  <done>e2e-phase7.sh passes the full flow (trigger→ack→comment→photo→note) plus the 401 (no-cookie subscribe), tenant-isolation 404 (foreign photo), and 400 negatives; npm test green; phase 1/3/5/6 E2E suites still green; README documents the demo + HTTPS + VAPID + E2E.</done>
</task>

</tasks>

<verification>
- Dev server on a unique port with demo seed (then stopped): three pages 200 with Norwegian markers; proxy redirects unauth /skift while keeping manifest/sw.js public.
- `zsh scripts/e2e-phase7.sh` → 0 failed (full flow + 401/403/404/400 negatives + tenant isolation).
- `npm test` green; regression: e2e-phase1.sh, e2e-phase3.sh, e2e-phase5-walkthrough.sh, e2e-phase6 suite all green.
- `npm run demo:setup` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Operator can install the app, enable notifications, receive a deep-linked push, and from /skift or /stopp/[id] acknowledge, comment (with reason correction), attach a camera photo, and write a shift note — all tenant-isolated.
- Full Phase 7 flow proven by e2e-phase7.sh; no regression in prior phase suites.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pwa-operator/07-05-SUMMARY.md`.
</output>
