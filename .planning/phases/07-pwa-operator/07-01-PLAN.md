---
phase: 07-pwa-operator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/seed.ts
  - src/lib/api-auth.ts
  - .gitignore
  - .env.example
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Five new tables (push_subscriptions, photos, stop_acknowledgements, stop_comments, shift_notes) exist after db:push"
    - "demo:setup runs green with the new schema (seed deletion order is FK-safe)"
    - "getApiSession() returns the session payload for a valid cookie and null (no redirect) for a missing/invalid cookie"
    - "Uploaded photo files under ./uploads are git-ignored"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "pushSubscriptions, photos, stopAcknowledgements, stopComments, shiftNotes tables with tenantId NOT NULL FK and Unix-seconds timestamps"
      contains: "export const pushSubscriptions"
    - path: "src/lib/api-auth.ts"
      provides: "getApiSession() — cookie → decrypt → payload | null (no redirect)"
      exports: ["getApiSession"]
    - path: "src/db/seed.ts"
      provides: "FK-safe deletion list extended for the 5 new tables"
      contains: "stopAcknowledgements"
    - path: ".env.example"
      provides: "VAPID key placeholders"
      contains: "VAPID_PUBLIC_KEY"
    - path: ".gitignore"
      provides: "/uploads ignore rule"
      contains: "/uploads"
  key_links:
    - from: "src/lib/api-auth.ts"
      to: "src/lib/session.ts decrypt()"
      via: "import + cookie read"
      pattern: "decrypt\\("
    - from: "src/db/schema.ts new tables"
      to: "tenants.id / users.id"
      via: "references() FK"
      pattern: "references\\(\\(\\) => (tenants|users)\\.id\\)"
---

<objective>
Lay the data + auth foundation every other Phase 7 plan builds on: five new tenant-scoped tables, an FK-safe seed deletion order, the API-session helper that route handlers use instead of the redirecting verifySession(), and the uploads/.env scaffolding.

Purpose: Push subscriptions, photos, acknowledgements, comments and shift notes all need tables and a non-redirecting auth path before any API or UI can be written. This is the Wave 1 data slice (runs in parallel with 07-02 PWA shell — no file overlap).
Output: Updated schema.ts (5 tables), seed.ts (deletion order), new src/lib/api-auth.ts, .gitignore + .env.example updates, schema pushed to the dev DB.
</objective>

<execution_context>
@/Users/magnushj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/magnushj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pwa-operator/07-RESEARCH.md
@src/db/schema.ts
@src/db/seed.ts
@src/lib/session.ts
@src/lib/definitions.ts
@src/app/api/auth/login/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five Phase 7 tables to the schema</name>
  <files>src/db/schema.ts</files>
  <action>
Append five new tables at the end of src/db/schema.ts, following the EXISTING patterns in this file exactly:
- Use `int()` / `integer()` / `text()` from drizzle-orm/sqlite-core (already imported), plus `index`/`uniqueIndex`.
- Every table: `id: int().primaryKey({ autoIncrement: true })`, `tenantId: int('tenant_id').notNull().references(() => tenants.id)`, and a `createdAt: integer({ mode: 'timestamp' }).notNull().$defaultFn(() => new Date())`.
- IMPORTANT (research pitfall #3): timestamps stored via `integer({ mode: 'timestamp' })` are persisted as Unix SECONDS by better-sqlite3 (NOT ms) — do not invent ms columns. Match the existing tables.
- Add a `tenant_idx` index on tenantId for each, mirroring the existing tables.

Tables (column names follow the snake_case-via-explicit-name + camelCase-property convention already used in this file):

1. `pushSubscriptions` → table('push_subscriptions'):
   - tenantId (FK tenants), userId: int('user_id').notNull().references(() => users.id)
   - endpoint: text().notNull()  — UNIQUE
   - p256dh: text().notNull(), auth: text().notNull()
   - userAgent: text('user_agent')  (nullable)
   - createdAt
   - indexes: uniqueIndex('push_subscriptions_endpoint_idx').on(t.endpoint), index('push_subscriptions_tenant_idx').on(t.tenantId)

2. `photos` → table('photos'):
   - tenantId (FK tenants), userId: int('user_id').notNull().references(() => users.id)
   - filePath: text('file_path').notNull()
   - mimeType: text('mime_type').notNull()
   - sizeBytes: int('size_bytes').notNull()
   - createdAt
   - index('photos_tenant_idx').on(t.tenantId)

3. `stopAcknowledgements` → table('stop_acknowledgements'):
   - tenantId (FK tenants), stopEventId: int('stop_event_id').notNull().references(() => stopEvents.id), userId: int('user_id').notNull().references(() => users.id)
   - createdAt
   - indexes: uniqueIndex('stop_ack_event_user_idx').on(t.stopEventId, t.userId), index('stop_ack_tenant_idx').on(t.tenantId)
     (the unique index enforces idempotent ack per user+stop — REPT-01)

4. `stopComments` → table('stop_comments'):
   - tenantId (FK tenants), stopEventId: int('stop_event_id').notNull().references(() => stopEvents.id), userId: int('user_id').notNull().references(() => users.id)
   - comment: text()  (nullable)
   - correctedReason: text('corrected_reason')  (nullable)
   - photoId: int('photo_id').references(() => photos.id)  (nullable FK — declare `photos` BEFORE this table so the reference resolves)
   - createdAt
   - indexes: index('stop_comments_event_idx').on(t.stopEventId), index('stop_comments_tenant_idx').on(t.tenantId)
   - Note: the "comment OR correctedReason required" rule is a ZOD rule enforced at the API layer (07-04), NOT a DB constraint.

5. `shiftNotes` → table('shift_notes'):
   - tenantId (FK tenants), plantId: int('plant_id').notNull().references(() => plants.id), userId: int('user_id').notNull().references(() => users.id)
   - content: text().notNull()
   - photoId: int('photo_id').references(() => photos.id)  (nullable)
   - createdAt
   - indexes: index('shift_notes_plant_time_idx').on(t.plantId, t.createdAt), index('shift_notes_tenant_idx').on(t.tenantId)
   - No shiftId FK — shift is derived from plantId + createdAt via src/lib/time.ts (consumer's concern, not schema).

Declaration ORDER matters for FK references resolving at module eval: photos must be declared before stopComments and shiftNotes. stopEvents, plants, users, tenants already exist above.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -i "schema.ts" || echo "schema typechecks"</verify>
  <done>schema.ts exports pushSubscriptions, photos, stopAcknowledgements, stopComments, shiftNotes; each has tenantId NOT NULL FK; stop_acknowledgements has a unique(stopEventId,userId) index; endpoint is unique on push_subscriptions; tsc shows no schema errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the seed deletion list (FK-safe) and push the schema</name>
  <files>src/db/seed.ts</files>
  <action>
In src/db/seed.ts, the cleanup block (currently starts `db.delete(schema.timeSeriesReadings).run()`) deletes rows leaves→parents before re-seeding. Prepend the five new tables in FK-safe order so demo:setup stays green (research: "stopAcknowledgements → stopComments → shiftNotes → photos → pushSubscriptions → (existing list)").

Add these lines BEFORE the existing first delete (`db.delete(schema.timeSeriesReadings).run()`):
```
db.delete(schema.stopAcknowledgements).run()
db.delete(schema.stopComments).run()
db.delete(schema.shiftNotes).run()
db.delete(schema.photos).run()
db.delete(schema.pushSubscriptions).run()
```
Rationale for this exact order: stop_comments references photos (must delete comments before photos); shift_notes also references photos; stop_acknowledgements/comments reference stop_events (delete before the existing stopEvents delete, which they already precede). Deleting acks/comments/notes first, then photos, then push_subscriptions satisfies every FK.

Do NOT add any new seed DATA — Phase 7 tables start empty; the demo populates them at runtime via the dev trigger and UI.

After editing, run drizzle-kit push to create the tables in the dev DB, then run demo:setup to confirm green.
  </action>
  <verify>npm run db:push 2>&1 | tail -5 && npm run demo:setup 2>&1 | tail -5</verify>
  <done>db:push reports the 5 new tables created (no errors); demo:setup completes with "Seed complete:" and no FK-constraint failures.</done>
</task>

<task type="auto">
  <name>Task 3: Create getApiSession() + uploads/.env scaffolding</name>
  <files>src/lib/api-auth.ts, .gitignore, .env.example</files>
  <action>
Create src/lib/api-auth.ts — the non-redirecting session reader for ROUTE HANDLERS (research pitfall #5: verifySession() in dal.ts calls redirect() which returns an HTML redirect, wrong for JSON APIs and breaks curl-E2E).

```ts
import 'server-only'
import { cookies } from 'next/headers'
import { decrypt } from './session'
import type { SessionPayload } from './definitions'

/**
 * Read + verify the session cookie for ROUTE HANDLERS.
 * Returns the payload on success, or null on missing/invalid cookie.
 * Unlike verifySession() (dal.ts) this NEVER redirects — callers return
 * NextResponse.json({error}, {status:401}) themselves.
 */
export async function getApiSession(): Promise<SessionPayload | null> {
  const cookie = (await cookies()).get('session')?.value
  return decrypt(cookie)
}
```
This is `server-only` and uses `next/headers` cookies() — fine, because it only runs inside route handlers (request context). Do NOT use it in the notifier (07-03), which runs outside request context.

.gitignore: add an `/uploads` rule. Add it under a clear comment. The existing `*.db` block is a good neighbour:
```
# uploaded operator photos (served only via authenticated /api/photos/[id])
/uploads
```

.env.example: append VAPID placeholders so the missing-keys path is documented and `cp .env.example .env.local` is a clear starting point:
```
# Web push (generate real keys with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:demo@steco.no
```
Do NOT generate real keys here — 07-03 owns key generation into .env.local (it owns package.json / web-push).
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -i "api-auth" || echo "api-auth typechecks"; grep -q "/uploads" .gitignore && echo "uploads ignored"; grep -q "VAPID_PUBLIC_KEY" .env.example && echo "vapid documented"</verify>
  <done>src/lib/api-auth.ts exports getApiSession returning SessionPayload|null with no redirect call; .gitignore contains /uploads; .env.example contains the three VAPID_* keys.</done>
</task>

</tasks>

<verification>
- `npm run db:push` creates all 5 tables with no FK errors.
- `npm run demo:setup` finishes green (seed deletion order is FK-safe).
- Round-trip insert/select smoke test across all 5 new tables via tsx + better-sqlite3 succeeds (executor should run a quick inline `npx tsx -e` snippet inserting one row in each table for tenant 1 and selecting it back, then deleting — mirroring the e2e-phase5 inline-tsx pattern).
- `npx tsc --noEmit` shows no errors in the touched files.
</verification>

<success_criteria>
- 5 tables exist, each tenant-scoped with Unix-seconds timestamps.
- stop_acknowledgements enforces unique(stopEventId,userId); push_subscriptions enforces unique(endpoint).
- getApiSession() returns payload for valid cookie, null for missing/invalid — never redirects.
- demo:setup is green; /uploads ignored; VAPID placeholders documented.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pwa-operator/07-01-SUMMARY.md`.
</output>
